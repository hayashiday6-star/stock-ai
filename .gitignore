# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# --- uv / venv ---
.venv/
uv.lock.bak

# --- Test / coverage ---
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.ruff_cache/

# --- Secrets & local config ---
.env
.env.*
!.env.example
設定.txt
# 立花の仮想URLを復号する秘密鍵。.env と同じ扱い。
*.pem
tachibana_private*

# --- Data / logs / DB (runtime artifacts) ---
logs/*
!logs/.gitkeep
data/*.db
data/*.sqlite
data/*.sqlite3
*.db-journal
# プローブが書き出す生の応答（株価のみ。個人情報は含まない）。
tachibana_history*.json
# 当日の仮想URL。復号済みなので、これ自体が資格情報。
tachibana_session*.json
# 登録用に書き出した公開鍵（秘密ではないが、生成物なので追跡しない）。
tachibana_public*.txt
# EDINET から落とした書類本体（公開情報だが、生成物なので追跡しない）。
edinet_probe/

# --- IDE / OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db
