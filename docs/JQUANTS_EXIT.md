# J-Quants 有料プラン解約：残っている穴

**期限 2026-09-22。** この日を過ぎると、有料プランでしか取れないものが取れなく
なる。**何が取れなくなるかを、解約する前に確定させておく**ためのメモである。

解約してから「これが無いと動かない」と分かるのが、いちばん困る形になる。

---

## 1. すでに埋まっているもの

| データ | 移行先 | 設定 | 状態 |
|---|---|---|---|
| 日足の価格 | 立花証券 e支店 API | `JP_PRICE_SOURCE=tachibana` | **実装済み・稼働中** |
| 財務諸表の時系列 | EDINET 有価証券報告書 | `JP_STATEMENT_SOURCE=edinet` | **実装済み** |

### 2026-09-03 に確認できたこと

`docs/TACHIBANA.md`「まだ確認していないこと」の1項目が消えた。

> **v4r10 での株価履歴。** `CLMMfdsGetMarketPriceHistory` は時価情報機能なので
> 今回の変更の対象外と思われるが、確認していない。

SUE 版の検証を回した際、ベンチマーク 1306 を **v4r10 で取得できている**。

```
立花から 1306 の日足 6156 件（2001-07-13〜2026-09-02）
```

**v4r10 で株価履歴は動く。** 確認済みに移してよい。

---

## 2. まだ埋まっていないもの

### ★銘柄マスタ・セクター・市場区分（代替なし）

`src/stock_ai/data/universe.py` と `src/stock_ai/data/jquants_profile.py` が
**どちらも** `https://api.jquants.com/v2/equities/master` を叩いている。
`JP_PRICE_SOURCE` / `JP_STATEMENT_SOURCE` はここに効かない。**設定で切り替え
られる形になっていない。**

影響するコマンド:

- `stock-ai universe`（銘柄一覧の取得と保存）
- `stock-ai factor-test --universe fresh`（`JQuantsUniverse(...).profiles()`）

`docs/TACHIBANA.md` は「株価と銘柄マスタは立花で埋まる」と書いていたが、
**2026-09-03 まで銘柄マスタ側は実装されていなかった。** 同ファイルの「まだ確認していないこと」に
その理由がある。

> **銘柄マスタ `CLMIssueMstKabu`**。v4r10 で取得方式が変わるため、v4r9 の
> 配信方式は実装しない。v4r10 のマニュアル公開（2026-08-29）を待つ。

**2026-09-03、実測して埋めた。** `JP_UNIVERSE_SOURCE=tachibana` で切り替わる。

| J-Quants で使っていたもの | 立花の代わり |
|---|---|
| 銘柄一覧 | `CLMStkGetIssueMstKabu` |
| セクター | 同上の `sGyousyuCode`（東証33業種） |
| 市場区分 | `CLMStkGetIssueSizyouMstKabu` の `sZyouzyouKubun`（01プライム/02スタンダード/09グロース） |
| 発行済株式数 | 同上の `sZyouzyouHakkouKabusu` |

### 実測（`checks\立花銘柄マスタ確認.bat`、本番、2026-09-03）

銘柄マスタ・銘柄市場マスタとも **4,441 件**。

| 上場区分 | 件数 |
|---|---|
| 01 プライム | 1,555 |
| 02 スタンダード | 1,555 |
| 09 グロース | 596 |
| 00 それ以外（ETF・REIT など） | 543 |
| 21 TPM（東京プロマーケット） | 187 |
| 03/04/11 外国銘柄 | 5 |

3区分の合計 3,706 は東証の公表値とほぼ一致する。**業種コードの空は 1 件だけ**
で、J-Quants と同じ4桁コードなので `sectors.from_tse33` がそのまま使える。

### ★上場廃止日は使えなかった

**4,441 件中 4,418 件が空**で、値が入っているのは 23 件だけだった。廃止済み
銘柄がマスタに残っているのではなく、**現存銘柄に廃止予定日が入っているだけ**
である。

**立花では生存バイアスを是正できない。** マスタは現存銘柄のみを返す。

これは「マニュアルに項目があること」と「過去のデータが取れること」が別だった
例である。`checks\立花銘柄マスタ確認.bat` はまさにこれを見つけるために書いた。

**ただし J-Quants でなら直せる（2026-09-03 に判明）。** 下の「★生存バイアスは
解約前なら直せる」を見ること。立花で無理だったことと、どこでも無理なことは
別だった。

詳細は `docs/TACHIBANA.md`「銘柄マスタ」。仮想ＵＲＬは `sUrlMaster` で、
株価履歴の `sUrlPrice` とは別の口である。

**ただし即座には壊れない。** 銘柄マスタは DB に保存済みで（3,607銘柄）、
既存の銘柄を使う分には J-Quants を叩かない。壊れるのは**更新**である
— 新規上場・上場廃止・市場区分の変更が反映されなくなる。

### ★生存バイアスは解約前なら直せる — 期限つきの作業が1つ増えた

実測（`checks\生存バイアス確認.bat`、本番、2026-09-03）。

`equities/master` は `date` を取り、**その日時点の一覧**を返す。日付ごとの
名簿が取れるということは、名簿同士の差が廃止銘柄になる。

| 日付 | 返った件数 |
|---|---|
| 2021-06-01 | **400（断られた）** |
| 2021-10-01 | 3,785 |
| 2022-06-01 | 3,823 |
| 2023-06-01 | 3,881 |
| 2024-06-01 | 3,893 |
| 2025-06-01 | 3,790 |

5年ローリング窓の境界は **2021-06 と 2021-10 の間**にある。

名簿同士の差＝その期間に消えた銘柄:

| 期間 | 消えた |
|---|---|
| 2021-10 → 2022-06 | 49 |
| 2022-06 → 2023-06 | 68 |
| 2023-06 → 2024-06 | 84 |
| 2024-06 → 2025-06 | 106 |

**年 49〜106 件、しかも増えている。** 4年で 307 件。1銘柄あたり数千の
シンボル日を持つので、リバーサルのような銘柄日ベースの検証では無視できない。

そして肝心なのは、**消えた銘柄の株価が 5/5 で取れた**ことである。

```
1352  169本  2021-09-06 〜 2022-05-18
1413  155本  2021-09-06 〜 2022-04-22
1448  136本  2021-09-06 〜 2022-03-28
1737  132本  2021-09-06 〜 2022-03-22
1881  136本  2021-09-06 〜 2022-03-28
```

銘柄コードだけ分かっても価格が無ければ何もできないので、ここが本題だった。

**したがって生存バイアスは恒久的な制約ではなく、期限つきの作業である。**

- 直せるのは **2021-09 以降だけ**。それ以前は、いま実行しても窓の外で返らない。
- 解約日 **2026-09-22 を過ぎると、名簿も廃止銘柄の株価も二度と取れない**。
- 実行するのは `checks\廃止銘柄の取り込み.bat`（`stock-ai delisted-harvest`）。
  取得元を **J-Quants に固定**してある。立花のマスタには廃止銘柄が無いので、
  `JP_PRICE_SOURCE` を見に行くと何も集まらないまま成功したように見える。

保存するのは**日付ごとの名簿**であって和集合ではない。和集合しか持たないと、
2023年に上場した会社を2021年の分位に入れられてしまう。生存バイアスを直した
つもりで先読みを持ち込むことになり、直す前より悪い。

名簿は `data/universe_snapshots/` に1日1ファイルで入る。**これは取り直せない
データなので git にコミットして残す。** DB（株価）は立花から作り直せるが、
名簿は解約後どこからも作り直せない。

### 過去に一度間違えた数え方

最初は「スナップショットと **DB** の差」を廃止数として数えた。**DB は上場一覧
ではない**ので、これは「廃止された」と「そもそも取り込んでいない」を混ぜる。
実際 DB 3,609 に対し 2025-06 の名簿は 3,790 で、差 181 は廃止数ではなく
主に取りこぼしだった。廃止を数えるには**名簿同士**を比べる。

### 全銘柄を立花で回したときの所要時間と転送量 — 実測済み、問題なし

実測（`checks\立花スループット計測.bat`、本番、2026-09-03、5銘柄）。

| | 値 |
|---|---|
| 1銘柄あたり | **1.23 MB / 0.21 秒**（約 6,200 本） |
| 3,607 銘柄への外挿 | **4.4 GB / 13 分** |
| 要求間隔を 0.5 秒空けた場合 | 43 分 |

**解約の障害にならない。** 外挿は線形で、DB書き込み・再試行・レート制限は
見ていないので実際にはこれより悪くなるが、桁が変わるほどではない。

---

## 3. 解約で失われ、どこにも代替が無いもの

移行先を用意しても戻ってこないデータがある。**「移行できた」と「同じものが
取れる」は違う。**

| 失われるもの | J-Quants の項目 | EDINET にあるか |
|---|---|---|
| **会社の通期予想（時系列）** | `FSales` / `FOP` / `FNP` / `FEPS` | **無い。** 有報は実績のみ |
| **開示時刻** | `DiscTime` | **無い** |
| 決算短信そのもの | `fins/summary` | 無い。EDINET は有報・四半期報告書 |

会社予想が取れなくなると、次が動かなくなる。

- `stock-ai sue-census` / `revision-census`（予想と実績の突き合わせ）
- `stock-ai pead-run --surprise sue`

開示時刻が取れなくなると、次が動かなくなる。

- `stock-ai pead-run`（反応日 R が場中か引け後かを決められない）

**予想EPS だけは立花で部分的に埋まる。** `CLMMfdsGetIssueDetail` の `pEPSF`
（一株利益・予想・通期連結）が取れる。ただし**スナップショットであって時系列
ではない**ので、過去の予想は遡れない。今日から貯め始めることはできる。

**決算ドリフトのテーマは2026-09-03に閉じた**（`docs/PREREG_SUE_JP.md`、
`docs/PREREG_PEAD_JP.md` ともに不合格）ので、この2つが止まっても現在の
検証計画には影響しない。**ただし DB に入っている分より後は増えない。**
将来この線を再開するなら、その時点で別の入手経路が要る。

---

## 3-2. 何が残っているかを、推測ではなく数える

`checks\解約前の棚卸し.bat`（`stock-ai jquants-inventory`）が、上の表を**いまの
DB に照らして**数える。取得はしない。

判断の分かれ目は1つだけである。**「解約後に作り直せるか」。**

| | 作り直せるか | 急ぐか |
|---|---|---|
| 日足の価格（現存銘柄） | 立花から何度でも | いいえ |
| 財務諸表 | EDINET から何度でも | いいえ |
| **日付ごとの上場名簿** | **どこからも無理** | **はい** |
| **上場廃止銘柄の株価** | **どこからも無理** | **はい** |
| **会社の通期予想・開示時刻** | **どこからも無理** | 決算ドリフトを再開するなら |

**5年ローリング窓は解約より先に効く。** いま取れるのは 2021-09 以降で、その端は
解約日を待たずに毎日後ろへ動く。「解約日まで待てる」ものは1つも無い。

棚卸しは「名簿にあって株価が無い銘柄」を数える。**これがそのまま、生存バイアスの
直っていない残りである。** 取り込みが途中で止まっても、この数字を見れば分かる
（銘柄だけ作られて株価が無い状態と、取り込み済みを取り違えない）。

---

## 3-3. 429 が銘柄ごとの失敗に化けていた（2026-09-04 に判明・修正）

廃止銘柄399件の取り込みで **84 件を取った直後にレート制限に当たり、残り 315 件が
2分で「失敗」した。そのどれ1つも実際には取りに行っていない。** 閉じた扉に約300回
叩きつけた形になる。

`BulkIngester` には対策が入っていた。`bulk.py` にはこう書いてある。

> 継続すると残りの universe を数秒で同じ拒否に費やすことになり、それが
> 「1件も取れないまま実行が終わる」形である

**その通りのことが起きた。** 原因は経路の違いだった。

- 財務側は provider を直接呼ぶので `RateLimitError` がそのまま上がる
- **価格側は `IngestionService.ingest_prices` を通る。** そこの
  `except StockAIError` が `RateLimitError`（`DataError` の一種）を先に飲み込み、
  銘柄ごとの失敗に変えていた

テストは財務側だけを見ていたので、価格側は素通りしていた。同じ現象を記録した
テストの docstring まであったのに、片方の経路にしか効いていなかった。

修正は `ingest_prices` で `RateLimitError` を先に通すだけ。価格側にも回帰テストを
足した（外すと本番と同じ「全件失敗」が再現する）。

---

## 3-4. 解約後の生存バイアスは、自前で直す

J-Quants の日付つき名簿は 2026-09 で止まる。それ以降どうするか。

**立花のマスタを月に1枚ずつ保存する。** マスタは現存銘柄しか返さないが、それは
1枚だけ見た場合の話で、**2枚あれば差分が「その月に消えた銘柄」になる。**

| | |
|---|---|
| 置き場所 | `data/tachibana_snapshots/YYYY-MM.csv`（git 追跡） |
| いつ書くか | `4-日次自動化` から毎日呼ぶ。その月の分があれば書かない |
| コマンド | `stock-ai universe-snapshot` |

**ファイル名を月にしてあるので、毎日呼んでよい。**「毎月やる」と覚えておく必要の
ある運用は、いずれ忘れられる。忘れた月は二度と埋まらない。

**J-Quants の名簿と同じフォルダに置かない。** 除外の仕方が違う（J-Quants は
投信・指数を396件除き、立花は上場区分で絞る）ので、境目をまたいで差を取ると
**消えてもいない銘柄が「消えた」ことになる。** 混ぜるなら、意図的に、対応を
確かめてから。

1年後には13枚たまる。コストはほぼゼロである。

---

---

## 3-5. 一括ダウンロードという経路がある（2026-09-04 に判明）

3-3 で直したのは「レート制限を銘柄ごとの失敗に化けさせない」ことで、**リクエスト
数そのものは減っていない。** 財務取得は `params = {"code": symbol}` で銘柄ごとに
1回、3,700銘柄なら3,700回である。

J-Quants 自身の資料が、この形を「よくある間違い」として名指ししていた。

> 全銘柄ループで `eq daily --code X` を繰り返す →
> `bulk get --endpoint /equities/bars/daily --date YYYY-MM --download`

**`/fins/summary` は一括対応である。** 解約前に取り切りたい会社予想
（FSales/FOP/FNP/FEPS）と開示時刻（DiscTime）は、ここに乗っている。

| | |
|---|---|
| 一覧 | `GET /v2/bulk/list`（`endpoint` / `date` / `from` / `to`、ページングあり） |
| URL取得 | `GET /v2/bulk/get`（`key` か `endpoint`+`date`。**排他**） |
| 返るもの | `{Key, LastModified, Size}` の一覧、または署名付きURL 1本 |
| 認証 | `x-api-key` — **既存の取得経路と同じ** |
| 形式 | gzip 圧縮の CSV。自動展開はされない |
| プラン | Light 以上（Free は `mkt calendar` を除いて不可） |

**専用の CLI を入れる必要は無い。** 認証が同じなので、いまの Python
クライアントからそのまま叩ける。鍵の写しがディスクに増えることもない。

### 署名付きURLの寿命は5分

一覧を全部取ってからまとめて落とすと、後ろのURLが死ぬ。**1本ずつ「取得して
すぐ落とす」**しかない。取り込みを書くときにここを外すと、途中から 403 が並ぶ。

### プラン別の上限と、遡れる年数

同梱の skill（`.claude/skills/jquants-cli-usage/`、`npx skills add
J-Quants/jquants-cli` で入る）に載っている。

| プラン | 1分あたり | 遡れる年数 |
|---|---|---|
| Free | 5 | 12週〜2年12週（CSV/一括は不可） |
| Light | 60 | 5年 |
| Standard | 120 | 10年 |
| Premium | 500 | 20年 |

**`BulkIngester` の既定 `throttle_seconds=0.5` は 120回／分である。** Standard の
上限ちょうどで余裕が無く、Light なら上限の2倍にあたる。**廃止銘柄の取り込みが
84件で止まったのは、これで説明が付く。** 3-3 で直したのは「429を銘柄ごとの失敗に
化けさせない」ことで、速すぎること自体は直していない。

大幅超過が続くと**約5分アクセスが完全に遮断される**。総時間で見れば、2割遅く
叩くほうが5分止まるより速い。

遡れる年数はプランで決まるので、**一覧が覆っている期間を見ればプランが分かる。**
`jquants-bulk-list` はそれを出す。契約内容を思い出してもらうより、返ってきた
ファイルを数えるほうが速い。

### 個別APIも銘柄ごとである必要は無かった

`fins summary` は `--date` を受ける（その日に開示した全銘柄が返る）。うちは
`params = {"code": symbol}` しか送っていない。一括が使えない場合でも、
日付で回すという道がある。

### 実測（`checks\一括ダウンロードの下見.bat`、本番、2026-09-04）

| エンドポイント | 本数 | 覆っている範囲 | 合計 |
|---|---|---|---|
| `/fins/summary` | 83 | 2021-09 〜 2026-09 | **9 MB** |
| `/equities/bars/daily` | 83 | 2021-09 〜 2026-09 | 119 MB |

ファイル名は2通りある。

```
fins/summary/historical/2021/fins_summary_202109.csv.gz   月（6桁）
fins/summary/live/fins_summary_20260904.csv.gz            日（8桁）
```

**5年ぶん。つまり契約は Light（1分あたり 60回）である。** 覆っている年数が
プランの区切りそのものなので、契約内容を思い出さなくても数字から出る。

> **`BulkIngester` の既定 0.5 秒は 120回／分で、Light の上限のちょうど2倍。**
> 廃止銘柄の取り込みが84件で止まった理由がこれである。

**会社予想と開示時刻は 83本・9MB で全部入る。** 銘柄ごとなら 3,700 リクエスト、
一括なら 166（1本につきURL取得と本体で2回）。`checks\財務を一括で取り切る.bat`。

### `NC` は単体だった。接頭辞なしが連結である（2026-09-04 に確認）

`fins.summary` のフィールドには2つの系列がある。

| 系列 | 例 | 中身 |
|---|---|---|
| 接頭辞なし | `Sales` `OP` `OdP` `NP` `EPS` `TA` `Eq` `BPS` | **連結** |
| `NC` 付き | `NCSales` `NCOP` `NCNP` `NCEPS` `NCEq` `NCBPS` | 単体（NonConsolidated） |

うちのコードは接頭辞なしを読んでいる。**それで正しい。**

CLI 同梱のスキーマは `NC` の側に「連結実績」という見出しを付けていて、
そのまま信じると読み先を全部入れ替えるところだった。**見出しではなく、
数字で確かめた。**

`uv run stock-ai statements-show 7203`（2026-09-04）:

| FY | 手元の売上 | トヨタ公表の連結売上 |
|---|---|---|
| 2022 | 31.4兆 | 31.4兆 |
| 2023 | 37.2兆 | 37.2兆 |
| 2024 | 45.1兆 | 45.1兆 |

単体なら12兆台になる。**桁の大小どころか、値そのものが各年で一致した。**
持株会社を1社見れば足りる、という形の確認である。

### トヨタ 2022年の1株配当が、同じ行の EPS と噛み合わない（未解決）

上の確認のついでに見つかった。**期限作業ではないので追っていない。**

| FY | 1株配当 | EPS | 配当性向 |
|---|---|---|---|
| **2022** | **148.00** | 205.23 | **72%** |
| 2023 | 60.00 | 179.47 | 33% |
| 2024 | 75.00 | 365.94 | 20% |
| 2025 | 90.00 | 359.56 | 25% |
| 2026 | 95.00 | 295.25 | 32% |

2022 だけ突出している。トヨタの FY2022 の配当は分割後で 52円なので、148 は
そちらとも合わない。**同じ行の EPS・BPS・株式数は分割後の尺度で公表値と
一致している**ので、その行の中で配当だけ尺度が違うことになる。

読んでいるのは `DivAnn`（当期実績の年間配当）。分割は 2021-09-30 実施で、
FY2022（2021-04〜2022-03）はそれをまたぐ。**分割前後で尺度の違う値を
組み合わせる**——このプロジェクトが最初に挙げている型そのものである。

一括取り込みはこの行を上書きする（`upsert_reports` は値のある列を書き換える）
ので、**直るか残るかがそこで分かる。** 残ったら、`DivAnn` の分割調整を疑う。

#### `restated()` はこれを直せない（2026-09-05）

まだ原因は確かめていないが、**直し方の側は確かめた。直せない。**

`restated()` が揃えられるのは、1期まるごとが1つの尺度に乗っているときだけで
ある。分割の年の年間配当が「分割前の中間配当 ＋ 分割後の期末配当」だとすると、
**その1つの値の中で尺度が混ざっている。** 148 のままでも、5で割った 29.6 でも
正しくない。しかも `restated()` はその期を最新の尺度とみなすので、**倍率1で
そのまま通す。** 例外は出ない。

そこで直さず、**名指しする**ようにした。`dividends_crossing_a_split()` が
「株式数が整数倍になった期のうち、年間配当を持つもの」を返し、取り込みが銘柄
ごとに1度だけ警告を出す。期末配当しか出さない会社ならまたいでも混ざらないので、
**これは疑いであって断定ではない。** 返すのは確かめる価値のある期である。

警告を `restated()` 側に置かなかったのは、あれが増配率・CAGR・連続増配の
それぞれから呼ばれるためで、同じ行が1銘柄で何度も並ぶ。

### 実測（`checks\財務を一括で取り切る.bat -Limit 3`、本番、2026-09-04）

最初の3本（2021-09/10/11）で通した。

| 段 | 数 |
|---|---|
| 読んだ行 | 4,997 |
| 書いた財務諸表 | 4,675 |
| 触れた銘柄 | **3,883** |

**銘柄コードの読めない行は0だった**（出ていれば警告が出る）。列名の読み違いは
無い。1本あたり約2秒、3本で15秒。

**銘柄ごとの経路は84銘柄で止まっていた。3本で3,883銘柄に届いた。**

4,997 と 4,675 の差 322 は、同じ銘柄が同じ会計期を2回開示した分（訂正など）が
1本にまとまったものである。**この推測を人にさせないよう、段ごとの数を出す
ようにした。** 説明の付かない減り方があれば、そこで見える。

### `Eq`（純資産）を「自己資本」の列に入れていた（2026-09-04 に判明・修正）

全83本を入れたあと、トヨタ 7203 で**自己資本の列だけが全年で増えた。**
売上・利益・EPS・BPS・株式数は1円も動いていない。

| FY | 前 | 後 | 差 |
|---|---|---|---|
| 2022 | 262,460 | 271,548 | +9,088 |
| 2023 | 283,387 | 292,642 | +9,255 |
| 2024 | 342,210 | 352,393 | +10,183 |
| 2025 | 359,248 | 368,789 | +9,541 |
| 2026 | 399,189 | 410,201 | +11,012 |

**差の 0.91〜1.10兆は、非支配株主持分そのものである。**

J-Quants のスキーマは2つを別に持っている。

| | |
|---|---|
| `ShEq` | **自己資本**（親会社持分） |
| `Eq` | **純資産**（非支配株主持分を含む） |

うちは `_first(record, "Eq", "Equity")` を読み、それを「自己資本」として
表示していた。EDINET 側は
`EquityAttributableToOwnersOfParentIFRSSummaryOfBusinessResults`
＝親会社持分を入れる。**同じ列に、定義の違う2つの量が入っていた。**

しかも同じ行の BPS は親会社持分ベース（26.2兆 ÷ 13.8億株 ＝ 1,904.88円）の
ままなので、**1行の中で自己資本と BPS の定義が食い違っていた。**

この列は ROE（自己資本利益率）と PBR の分母に使われる。どちらも慣行として
自己資本を使うので、`ShEq` を先に読むよう直した。`ShEq` が無いときは `Eq` で
代用するが、**代用した件数を警告に出す。** 黙って混ぜるのが、この種の不具合が
高く付く理由である。

**直したあと入れ直して確認した（2026-09-04）。** 5年とも一括前の値に戻った。

| FY | 修正後 | 一括前（EDINET 由来） |
|---|---|---|
| 2022 | 262,460 | 262,460 |
| 2023 | 283,387 | 283,387 |
| 2024 | 342,210 | 342,210 |
| 2025 | 359,248 | 359,248 |
| 2026 | 399,189 | 399,189 |

**「元に戻った」以上のことが言える。** J-Quants の `ShEq` と EDINET の
`EquityAttributableToOwnersOfParent` は別々の経路で、突き合わせたわけでもない。
その2つが表示の桁まで一致した。`ShEq` が正しい読み先であることと、EDINET の
IFRS 側の対応が正しいことが、同時に確かめられたことになる。

### 同じ誤りが EDINET 側にもあった（2026-09-05 に確認・修正）

疑いとして書いてあったものを確かめた。**そのとおりだった。**

| 基準 | 要素名 | 中身 |
|---|---|---|
| IFRS | `EquityAttributableToOwnersOfParent…` | 親会社持分＝自己資本 |
| 日本基準 | `NetAssetsSummaryOfBusinessResults` | **純資産** |

つまり同じ `equity` 列が、**会計基準によって別の量を持っていた。** 一律に
間違っているより悪い——IFRS 適用会社と日本基準の会社を並べて比べたとき、差が
会計基準と相関する。

**日本基準の「主要な経営指標等」に、自己資本の絶対額は無い。** 実ファイル
（6501・7203・8306・9020）で確認した。あるのは純資産額・自己資本比率・
自己資本利益率・1株当たり純資産額の4つだけ。だから**自己資本比率×総資産**で
導く。両方とも同じ表の同じ期にある。

#### どちらが正しいかは、同じ表の ROE が決めた

J-Quants と突き合わせるつもりだったが、有報1本の中だけで決着が付いた。
三菱UFJ（8306）の四期前:

| | |
|---|---|
| 純資産額 | 17.99兆 |
| 自己資本比率 × 総資産 | **17.00兆** |
| 差 | **約1兆円**（非支配株主持分） |

親会社株主に帰属する当期純利益 1.131兆を、それぞれで割る。

| 分母 | 出る ROE | 報告された ROE |
|---|---|---|
| 純資産 | 0.0629 | 0.0668 |
| **自己資本** | **0.0665** | 0.0668 |

**残る 0.0003 は期中平均と期末値の違いで説明が付く。** 純資産側の 0.0039 は
説明が付かない。

JR東日本（9020）でも向きは同じで、差は 1.1% と小さい。**小さいから直さない、
にはしない。会社ごとに差が違うということは、直さなければ会社間の比較にその差が
そのまま乗るということである。**

導出できないとき（自己資本比率か総資産が欠けるとき）は純資産で代用するが、
**有報1本につき1行の警告を出す。**

### 直したはずの側が、半分しか直っていなかった（2026-09-05）

上の 2026-09-04 の修正は `normalize_statements`（財務諸表の系列）だけに入って
いて、**`normalize_statement`（スナップショット）は `Eq` を直に読んだままだった。**

同じ `records` から作る2つの出力で、`statements` は自己資本、`snapshot` は
純資産。`screen --max-pbr` はスナップショット側を読む。**例外は出ない。**

部品のテストは両側とも通っていた。無かったのは**同じ入力から出る2つの出力を
突き合わせるテスト**で、これは `factor_panel` と `build_estimators` の符号が
逆になっていた件と同じ形である。

### 先に測る。作らない

`checks\一括ダウンロードの下見.bat`（`stock-ai jquants-bulk-list`）は
**1バイトも落とさない。** 見るのは3つ。

1. **本数** — 月次なのか日次なのかが分かる
2. **覆っている期間** — 一括のほうが短ければ、乗り換えると行が減る
3. **合計サイズ** — 期限内に落とし切れる量か

2 が肝である。**一括に乗り換えて行数が減っても、例外は出ない。**
このプロジェクトで繰り返し起きているのは、まさにその形の不具合である。
粒度を推測して取り込みを書く前に、実物の本数と範囲を見る。

---

## 4. 解約前にやること

- [x] `checks\立花銘柄マスタ確認.bat` — 銘柄マスタが埋まっていることを実測した
- [x] `checks\立花スループット計測.bat` — 全銘柄 13 分で回ることを実測した
- [x] 銘柄マスタを立花に移した（`JP_UNIVERSE_SOURCE`）
- [x] `.env` に `JP_UNIVERSE_SOURCE=tachibana` を書いた
- [x] `uv run stock-ai info` — 3つとも `tachibana` / `edinet` / `tachibana`
      になっていることを確認した（2026-09-03）
- [x] `uv run stock-ai universe --segment prime` を立花で通した。
      プライム **1,549 件**（内訳は下記）
- [x] `checks\生存バイアス確認.bat` — 過去の名簿と廃止銘柄の株価が取れることを
      実測した（2026-09-03）
- [x] **`checks\解約前の棚卸し.bat` を実行した（2026-09-05）。** 数字は下の
      「棚卸しの実測」に控えた。**次回はそこと引き算する**
- [x] `checks\廃止銘柄の取り込み.bat` — **名簿は完了。** 62件を要求し 61件を取得
      （2021-10-01 〜 2026-09-04）。断られたのは 2021-09-01 の1件だけ
- [x] **廃止銘柄の株価を取り込んだ（2026-09-04）。** 取れなかったのは **16件**
      で、API が 200 を返すのに使える行が無い。**再試行では取れない**（4回とも
      同じ結果）。名簿 4,124 の 0.4%、消えた銘柄 527 の 3.0%
- [x] **2021-09-04 の名簿を取得。** 断り文面が名指しした境界。30日刻みでは
      最初の名簿が 2021-10-01 になっていたので、4週間ぶん多く取れた
- [x] **名簿を git にコミットした。** `data/universe_snapshots/` の63件
      （2021-09-04 〜 2026-09-04）。**解約後どこからも作り直せない唯一のデータ**
- [x] 落とした名簿（`data/universe_snapshots/`）をコミットした。上の項目と
      同じ作業で、二重に書いてあった
- [x] **`checks\一括ダウンロードの下見.bat` を実行した（2026-09-04）。**
      `/fins/summary` `/equities/bars/daily` とも 83本、2021-09 〜 2026-09。
      9 MB と 119 MB。**5年ぶん＝Light、1分あたり60回**。既定の 0.5 秒は
      その2倍だった
- [x] **`checks\財務を一括で取り切る.bat` は実行しないことにした（2026-09-05）。**
      銘柄ごとの経路で既に取り切っており、一括にしても同じ行を取り直すだけ。
      下の「取りに行かないと決めた」を参照
- [x] **`NC` が連結か単体かを確かめた（2026-09-04）。** 接頭辞なしが連結で、
      うちの読み先は正しい。トヨタ 7203 の売上が各年で公表の連結値と一致した。
      CLI 同梱スキーマの「連結実績」という見出しのほうが誤り
- [ ] （期限外）トヨタ 2022年の1株配当 148.00 が、同じ行の EPS と噛み合わない。
      分割をまたぐ年で尺度が違う疑い。一括取り込みで直るかどうかを見る。
      **2026-09-05: 原因は未確認のまま、`restated()` では直せないことが確定した。**
      混ざった値は倍率では直らない。名指しする警告を入れた（上記）
- [x] **`checks\会社予想と開示時刻を取り切る.bat` を実行した（2026-09-05）。**
      4,160 銘柄は既に最新、19 銘柄が新たに取れて 52 行。**残り 124 銘柄は
      J-Quants に財務が1行も無い。** 下の「取れない 124 銘柄」を参照
- [x] **立花マスタの月次スナップショットを始めた（2026-09-04）。**
      `data/tachibana_snapshots/` に `YYYY-MM.csv` を1枚ずつ。差分がそのまま
      「その月に消えた銘柄」になるので、**2026-10 以降の生存バイアスは自前で
      直せる**。`4-日次自動化` から毎日呼ばれ、その月の分があれば書かない

### 棚卸しの実測（2026-09-05）

**この表があるのは、次に数えたとき引き算できるようにするためである。**
`checks\会社予想と開示時刻を取り切る.bat` は「『うち会社予想あり』が増えて
いれば進んでいます」と言うが、**前回の値がどこにも書いていなければ比べられない。**

| | 2026-09-05 |
|---|---|
| 銘柄の登録 | 4,303 |
| 日足の価格 | 4,109 銘柄（2001-01-04 〜 2026-09-02） |
| 財務諸表 | 77,667 行 / 4,179 銘柄（2021-09-01 〜 2026-09-04） |
| 　うち開示時刻あり | 77,667 行（**100%**） |
| 　うち会社予想あり | 59,280 行（76%） |
| 日付ごとの名簿 | 63 件（2021-09-04 〜 2026-09-04） |
| 名簿にあって株価が無い | 16 銘柄 |

### 銘柄ごとの経路は、もう取り切っている（2026-09-05）

同じ実行の内訳。**19 ok / 4,160 skipped / 124 failed / 52 行。**

`fins/summary` は1リクエストで**その銘柄の5年ぶん全部**を返す（`fetch_statements`）。
だから「最新である」と判定された 4,160 銘柄には、その5年の行がすでに揃って
いる。**一括ダウンロードに切り替えても、同じ行を取り直すだけになる。**
3,700 リクエストを 166 に減らす利点は、そのリクエストを既に払い終えた今では
残っていない。

残る期限の実体は**一括か銘柄ごとかではなく、5年ローリング窓の前端**である。
2021-09 より前は既に取れない。後端は毎日伸びるので、**9/22 の直前にもう一度
回せば最後の2週間ぶんが入る。** それだけである。

### 取れない 124 銘柄（2026-09-05）

`No J-Quants statements for 'XXXX'.` が 124 件。**再実行しても取れない。**

表に出た20件を 2026-09-04 の名簿（J-Quants の `equities/master`）と突き合わせ
ると、**11件は現存する会社である。**

| | 件数（表示された20件のうち） |
|---|---|
| 名簿にある（現存） | 11 |
| 名簿に無い（廃止など） | 9 |

現存側は 動力・ひかりホールディングス・横浜ライト工業・ワイ・ティー・エル・
コーポレーション・バーハッド・コンピュータマインド・ファーストステージ・
アイダ設計・アーバンライク・アンサーホールディングス・ジェイレックス・
コーポレーション・清鋼材。建設業・不動産業・金属製品に偏った小型株である。

**「ETF や廃止銘柄だから取れないのだ」は違う。** そう考えていたが、名簿と
突き合わせたら半分以上が現存の普通の会社だった。

**なぜ名簿には載るのに財務が1行も無いのかは、まだ分かっていない。** 分かって
いるのは「再実行では取れない」ことだけである。124 銘柄は 4,303 の 2.9%。

失敗として毎回124行並ぶのは、**次に本物の失敗が出たときにそれが埋もれる**と
いう意味で困る。区別を付けるなら、この一覧を持っておいて「前回と同じ顔ぶれか」
だけを出すのが軽い。

### 取りに行かないと決めた（2026-09-05）

**9/22 までにもう一度データを取りに行くことは、しない。** 残り17日あるが、
使い道が無いまま取っておく作業になるため。

会社予想（`FSales`/`FOP`/`FNP`/`FEPS`）と開示時刻（`DiscTime`）は、決算ドリフト
のテーマのために要るものだった。**そのテーマは 2026-09-03 に閉じ（#2 と #3 が
両方落ちた）、因子探索そのものも 2026-09-05 に閉じた**（`docs/HYPOTHESES.md`
の 4'）。使う先が無い。

#### 何を失うかは、はっきりさせてから決めた

失うのは**2週間ぶんの行**である。それ以上ではない。

- 銘柄ごとの経路は既に取り切っている。4,160 銘柄が「最新」で、`fins/summary`
  は1リクエストで5年ぶんを返すので、その5年の行は揃っている
- 一括ダウンロードに切り替えても、同じ行を取り直すだけになる
- 5年ローリング窓の**前端**（2021-09 より前）は、いま回しても取れない。
  これは既に失われている
- 動くのは**後端**だけで、最後に取った 2026-09-04 から解約日 2026-09-22 まで
  の約2週間ぶん

**「取る判断には期限があり、捨てる判断は後からできる」**というのが、この
チェックリストを作ったときの理屈だった。それは正しいが、**期限までに取れる
ものが2週間ぶんしか無いと分かった時点で、天秤の片方が軽くなった。**

#### もし将来テーマを再開したくなったら

2021-09 〜 2026-09-04 の会社予想と開示時刻は**手元にある**（59,280 行 /
77,667 行）。抜けるのは 2026-09-05 以降だけである。**再開しても、過去5年を
使う検証はそのまま組める。**

新しい期の会社予想が要る形（予想修正を追いかけるような設計）に進むなら、
そのときは J-Quants を契約し直すか、別の入手先を探すことになる。**その判断を
いま先取りしない。**

### Standard に上げると何が増えるか（2026-09-06 に整理）

同梱の skill のプラン表（`.claude/skills/jquants-cli-usage/references/plans.md`）
から引き写した。**買うかどうかの判断材料であって、買う理由を探した一覧ではない。**

#### a. 遡れる年数が 5年 → 10年 になるもの

| | いま | Standard | **このプロジェクトでの意味** |
|---|---|---|---|
| `eq master` 上場銘柄一覧 | 5年 | 10年 | **名簿が 2016-09 まで。生存バイアスを直せる範囲が倍になる** |
| `fins summary` 財務情報 | 5年 | 10年 | **ほぼ重複。** EDINET が 2017-06 まで持っていることを実測済みで、そちらは無料・恒久 |
| `eq daily` 株価 | 5年 | 10年 | **効かない。** 立花から 2001-01 以降・4,109銘柄が既に手元にある |
| `mkt calendar` / `idx daily-topix` | 5年 | 10年 | 使っていない（ベンチマークは 1306 の株価で足りている） |

**「10年に伸びる」が実際に効くのは名簿だけである。** 株価は立花で足りており、
財務は EDINET とほぼ同じ範囲になる。`fins summary` に固有なのは四半期の粒度と
会社予想・開示時刻だが、**そのテーマは 2026-09-03 に閉じている。**

#### b. 新しく取れるもの（すべて10年前まで）

| コマンド | 中身 | 形 |
|---|---|---|
| `mkt margin-alert` | 日々公表信用取引残高（**規制の別つき**） | 候補9 そのもの |
| `mkt short-sale-report` | **空売り残高報告**（個別銘柄・大口） | イベント型。**候補10本に無い** |
| `mkt margin-interest` | 信用取引週末残高 | 週次の断面 |
| `mkt short-ratio` | 業種別空売り比率 | 業種単位。粗い |
| `idx daily` | 指数四本値 | 業種別指数など |
| `deriv options-225` | 日経225オプション | ボラ指数を自作する材料 |

#### b'. Premium まで上げると何が増えるか

**Premium 固有のもの**（Standard に無い）。

| コマンド | 中身 | このプロジェクトでの位置 |
|---|---|---|
| `fins details` | 財務諸表 BS/PL/CF（20年） | **因子の材料。閉じた方向** |
| `fins dividend` | 配当金情報（20年） | 候補5（権利落ち）。**クラスタ数が足りないと自分で書いてある** |
| `mkt breakdown` | 売買内訳データ（20年） | フロー。**機構の族としては近いが、日本での報告が乏しい** |
| `deriv futures` / `deriv options` | 先物・オプション（20年） | **別の資産クラス。** 契約の話ではなく設計の話になる |
| `eq am` | 前場四本値 | **直近のみ。履歴が無いのでバックテストに使えない**（ライブ用） |

**Premium 固有の中身は、記録が閉じた方向に偏っている。** `fins details` と
`fins dividend` は「もっと良い因子」の材料であり、7本の結論は「縛っているのは
因子ではなく設計である」だった。

#### b''. ただし1つ、性質の違う効き方がある — **名簿が20年**

`eq master` は Standard で10年、Premium で20年になる。**これは因子の材料では
なく、生存バイアスを直せる範囲である。**

52週高値で OOS を 2021-10 以降（1,197日）に切ったのは、**廃止銘柄の株価が
そこからしか無かったから**だった。名簿が伸びれば、そこが伸びる。

実測の SD（20日保有、6.47%、膨張2.29倍）をそのまま当てた算術。

| 名簿の範囲 | OOS に使える日数 | 必要な差 | ＋費用 |
|---|---|---|---|
| 5年（いま） | 1,197 | 0.86% | **1.26%** |
| 10年（Standard） | 約2,600 | 0.58% | **0.98%** |
| 20年（Premium） | 約4,800 | 0.43% | **0.83%** |

候補文書が「現実的な水準」と書いた線は **1イベント 1.0%** である。

**これは効果を大きくするのではなく、検出できる差を下げる。** 3つ挙げた
レバーのうち「期間」そのもので、そこは実測で 1.38倍 動いた唯一のレバーだった。

**#3 の話ではない。** #3 は封印前に閉じ、再測定しないと書いてある。これは
**次にイベント型を測るときの算術**である。

#### b'''. 20年ぶんを落とすのに何日の契約が要るか（2026-09-06）

**ダウンロードの時間では決まらない。**

実測（`docs/JQUANTS_EXIT.md`「一括ダウンロードの下見」2026-09-04）から。5年で
83本・`/fins/summary` 9MB・`/equities/bars/daily` 119MB。20年はその4倍。

| | |
|---|---|
| 1エンドポイントあたり | 約 332本、664リクエスト |
| 既定の 0.5 秒間隔（120回/分） | **5.5 分** |
| Premium の上限（500回/分） | 1.3 分 |
| `fins/summary` 20年 | 約 36MB |

**リクエスト数もサイズも制約にならない。転送は数時間で終わる。**

##### 決めるのは「取り込む口が無いこと」である

**`margin-alert` / `short-sale-report` / `fins details` / `fins dividend` /
`breakdown` のどれにも、読む口が1つも無い。** 実装済みなのは名簿・株価・
`fins summary` だけである。

契約を始めた瞬間から、**パーサを書いている時間が課金される。**

##### サンプルに全部ある。契約前に作れる

配布サンプル（`sample_data_v2`）に、上記すべてが**実物の形式**で入っている。

```
Daily Margin Interest.csv          = margin-alert
Outstanding Short Selling Positions Reported.csv = short-sale-report
Financial Statement Data(BSPLCF).csv = fins details
Cash Dividend Data.csv             = fins dividend
Breakdown Trading Data.csv         = breakdown
Margin Trading Outstandings.csv    = margin-interest
```

**パーサは1日も契約せずに書いてテストできる。** このプロジェクトの規則
（「新しいデータ取得ロジックは、可能な限り実データで検証する。fixtureは実物から
作る」）にも合う。

##### 日数の見積もり

**このセッションで通したデータ取得は、どれも1回では終わらなかった。**

| | 回数 |
|---|---|
| 名簿の取り直し（貸借区分） | **3回**（63件 → 65件 → 1件残り） |
| 財務の一括 | `-Limit 3` で試してから本番 |
| 立花マスタ | 件数が合わず原因を追った |

**「取れた」と「入った」が別だったことを、2日で2回踏んでいる。**

| 段 | 何をするか | 課金 |
|---|---|---|
| 0 | サンプルでパーサを書き、テストを通す | **無料** |
| 1 | `checks\一括ダウンロードの下見.bat` で本数・範囲・サイズを実測（1バイトも落とさない）。少数で試す | 契約1日目 |
| 2 | 本番のダウンロード | 2日目 |
| 3 | 検算、取りこぼしの回収 | 3〜5日目 |

**推奨は5日。最低3日。1日は勧めない。**

##### 先に確かめること — 期限が消えるかもしれない

**日割りで契約できるなら、9/22 より後に Premium を1週間だけ契約する道があるか。**

あるなら、**この件の期限は消える。** いま急いで決める理由が無くなり、パーサを
落ち着いて作ってから、必要になった時点で契約すればよい。

**確かめずに急がない。** これも「片方の経路で詰まったことを、どこでも無理と
書かない」と同じ形である。

#### c. この整理をどう使うか

**ここが動機づけられた推論の入口である。** 候補を3本続けて閉じた直後に
「何が買えるか」を数え上げると、買う理由のほうが見つかりやすい。

**どれも §0 を通していない。** 通るかどうかは測らないと分からず、測るには
データが要る。**「取ってから考える」は、判定の資源を使わない代わりに、
後戻りできない。**

言えることは4つだけである。

1. **名簿の年数は、他で代替できない。** 生存バイアスはこのプロジェクトが
   何日もかけて直してきたもので、10年で倍、20年で4倍になる。**そして
   検出できる差がその平方根ぶん下がる**（上の表）
2. **財務の10年は、ほぼ払う必要がない。** EDINET が同じ範囲を無料・恒久で持つ
3. **`short-sale-report` は候補10本に無かった。** 「制度が需給を作る」という、
   候補1・9 と同じ機構の族で、個別銘柄・イベント型である。**ただし日本での
   報告値は確かめていない**ので、§0 の段2（自分の IS から推定）になる
4. **Premium 固有の中身は、閉じた方向に偏っている。** 財務諸表の全項目も配当
   情報も「もっと良い因子」の材料で、7本の結論はそこではなかった。**Premium を
   選ぶ理由があるとすれば、固有の中身ではなく名簿の20年である**

**買うかどうかを、この一覧の長さで決めない。** 増えるものは数えられるが、
**どれも §0 を通していない。** 通るかは測らないと分からず、測るにはデータが
要る——その循環は、契約を上げても解けない。

### 通した結果の突き合わせ（2026-09-03）

| | 実測 | マスタからの見込み | |
|---|---|---|---|
| 東証3区分でない除外 | 730 | 543（00）+ 187（21） | ✓ |
| 5桁コードの除外 | 7 | — | 優先株・種類株 |
| **プライム** | **1,549** | 1,555（01）+ 1（03）− 7 | ✓ |

**すべての件数が説明できる。** 5桁コード（`25935` など、末尾5・先頭4桁は既存の
普通株コード）は優先株・種類株で、**落ちるのが正しい**。

この7件は最初のうち**数えずに落ちていた**。件数が合わない原因を推測するしか
ない状態だったので、落ちる経路すべてに数え口を置いた。

## Premium 1週間（2026-09-15 〜 09-22）の段取り

### 決まったこと（2026-09-06）

**日割りで後から契約する道は無い。** 変更できるのは 9/22 までで、それを過ぎ
たら Premium の20年は手に入らない。つまり b''' に書いた「期限が消えるかも
しれない」は**消えなかった**。

期間は **9/15〜9/22 の8日**。b''' の見積もり（推奨5日・最低3日）を満たす。

| 段 | いつ | 何をするか | 課金 |
|---|---|---|---|
| 0 | **9/14 まで** | サンプルで読み口を作り、テストを通す | **無料** |
| 1 | 9/15 | `JQUANTS_PLAN` を直す → 下見（1バイトも落とさない）→ 少数で試す | 1日目 |
| 2 | 9/16〜17 | 原本をまるごと保存 | 2〜3日目 |
| 3 | 9/18〜21 | 照合、取りこぼしの回収、名簿の20年 | 4〜7日目 |
| 4 | 9/22 | 解約（または Light に戻す） | 8日目 |

**段0 が間に合わないなら、段1 を後ろにずらすのではなく段0 を削る。** 期限は
9/22 で動かないので、後ろにずらすと段3 が消える。そして**この2日で「取れた」
と「入った」が別だったことを2回踏んでいる**（名簿の取り直しは3回、財務の一括
は試行のあと本番）。**削るなら段0、守るのは段3。**

### 9/15 に最初にやること — `.env` の `JQUANTS_PLAN`

```
notepad .env
JQUANTS_PLAN=Premium
```

**`APIキー設定.bat` では設定できない。** あれは秘密の値専用で、`ValidateSet`
に載っていない名前は受け付けない。`JP_PRICE_SOURCE` と混同して
`EDINET_API_KEY` を上書きした前例がある。

**なぜ設定が要るのか。** 遡れる年数はプランで決まる（Light 5年 / Standard
10年 / Premium 20年）。ここが `Light` のままだと、**例外も警告も出ないまま**
5年より前の日付を「窓の外だから取れない」と判断して要求を出さない。**20年
ぶん払って5年ぶんだけ落とす**ことになり、気付くのは解約したあとである。

`uv run stock-ai info` に `jquants_plan` と「遡れる: YYYY-MM-DD まで」を出す
のはこのためである。`JP_PRICE_SOURCE` で同じ間違いを既に踏んでいる。

**戻すのも忘れない。** 9/22 に Light へ戻すか解約したら、`JQUANTS_PLAN` も
戻す。上げっぱなしにすると、今度は逆に「取れるはず」と案内して、成功しない
`.bat` を何度も実行させる。

### 段0 の進み具合

| 読み口 | 元にした実物 | 状態 |
|---|---|---|
| `margin-alert`（日々公表信用残） | `Daily Margin Interest.csv` | **できた**（`jquants_margin.py`） |
| `short-sale-report` | `Outstanding Short Selling Positions Reported.csv` | まだ |
| `fins details` | `Financial Statement Data(BSPLCF).csv` | まだ |
| `fins dividend` | `Cash Dividend Data.csv` | まだ |
| `breakdown` | `Breakdown Trading Data.csv` | まだ |
| `margin-interest`（週末残） | `Margin Trading Outstandings.csv` | まだ |

`margin-alert` を先にしたのは、**候補9 の材料がこれだけだから**である。JPX も
日証金も過去分を配っていない（どちらもその日の断面だけ）。

### 原本をそのまま残す（段1〜2 の前提）

**取得は1回きり、解析は何度でもやり直せる。** パーサの誤りは10月にも見つかる
が、そのとき原本が無ければ取り返せない。名簿を CSV で残したのと同じ理屈で
ある。

| `.bat` | 何をするか |
|---|---|
| `checks\原本の下見.bat` | **1バイトも落とさない。** 本数と合計 MB を出す |
| `checks\原本をまるごと保存.bat` | 返ってきたバイト列をそのまま書く。展開しない |
| `checks\原本の照合.bat` | 目録と突き合わせる。**解約後こそ意味がある** |

保存は途中で止めても安全に再開できる。既にあって大きさの合うファイルは落とし
に行かず、**大きさが合わないものは落とし直す**——「ファイルがある」と「中身が
揃っている」は別で、転送が途中で切れても例外が出ないことがある。

目録（`manifest.csv`）は `size`（一覧が言う大きさ）と `bytes`（実際に届いた
大きさ）を**別の列**で持つ。同じ列に入れると、切れたダウンロードが「落とせ
た」ことになる。

### 下見の実測（2026-09-06、Light のまま）

`checks\原本の下見.bat`。**1バイトも落としていない。**

| エンドポイント | 本数 | 覆う範囲 | 合計 |
|---|---|---|---|
| `/equities/master` | 65 | 2021-09 〜 2026-09 | 135 MB |
| `/equities/bars/daily` | 64 | 2021-09 〜 2026-09 | 119 MB |
| `/fins/summary` | 64 | 2021-09 〜 2026-09 | 9 MB |
| `/equities/investor-types` | 61 | 2021-09 〜 2026-09 | 0 MB |
| **合計** | **254** | | **263 MB** |

残り13本は `DataError`。覆う範囲が5年であることから、Light である裏が取れた。

`investor-types` の 0 MB は四捨五入である。**「取れなかった」ではない**——
本数が 61 で範囲も出ている。

#### 20年ぶんの見積もり

| | |
|---|---|
| いま開いている4本を20年 | 約 1,016 本、**約 1 GB** |
| リクエスト数 | 約 2,000（Premium 500回/分なら数分） |
| Premium で新たに開く13本 | **測っていない。分からない。** |

**転送は制約にならない**（b''' と同じ結論）。ディスクは1 GB では足りない
——新しく開く13本のうち、デリバティブと売買内訳は日次で全銘柄なので、
株価より大きくなりうる。**9/15 の下見をもう一度回して、そこで実測する。**

#### エンドポイント名を6本間違えていた

**この下見で見つかった。取引カレンダーと TOPIX は Light でも取れるはずなのに
落ちていた。**

| 書いていた名前 | 正しい名前 |
|---|---|
| `/markets/trading-calendar` | `/markets/calendar` |
| `/indices/topix` | `/indices/bars/daily/topix` |
| `/indices/daily` | `/indices/bars/daily` |
| `/derivatives/futures` | `/derivatives/bars/daily/futures` |
| `/derivatives/options` | `/derivatives/bars/daily/options` |
| `/derivatives/options-225` | `/derivatives/bars/daily/options/225` |

`jquants` CLI の短い名前（`idx daily` / `deriv futures`）に引きずられている。
API のパスには `bars/daily` が入る。

**綴りが違うと `DataError` が返るが、それは「プランに入っていない」ときと
見分けが付かない。** Premium の週に同じことが起きれば、「Premium にも無いの
だ」と読んで取らずに終わる。そのときは契約が終わっていて、確かめ直せない。

直したのは名前だけではない。

1. **一覧を2つ持つのをやめた。** `ARCHIVE_ENDPOINTS` は `BULK_ENDPOINTS`
   から引く。手で写したから間違えた。
2. **落ちた理由を表に出す。** 以前は例外の型名（`DataError`）だけを出して
   いたので、13本が同じ見た目で並んでいた。**断られ方の中身は定型ではなく、
   判断の材料である。**

「プランで説明が付かない1本が混じっていたら、名前を疑う」を出力にも書いた。
