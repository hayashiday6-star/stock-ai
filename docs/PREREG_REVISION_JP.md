# 事前登録 — `REVISION_JP`「上方修正後のドリフト」（日本株）

**状態：§0 を測る前。封印していない。**（2026-09-16）

**判定を一度も使っていない説である。** 2026-09-03 に閉じたが、**閉じた理由が
事実として誤りだった**（2026-09-08 に訂正）。結果を見て止めたのではないので、
再開の筋が通る。

## なぜ一度閉じ、なぜ戻ってきたか

こう書いて止めた。

> 予想修正は**独立した開示として取得できない**。実測で `fins/summary` が返す
> 開示の 99.2% は決算短信で、**予想修正は上位12種類に1件も現れなかった。**

**銘柄ごとに叩く経路で数えていた。** その経路は 3,700銘柄で 429 に当たって
止まり、315銘柄は一度も取りに行けていない。**偏った一部を見て「1件も無い」と
読んだ。**

一括ファイルで数え直すと、`EarnForecastRevision` は **13,868 件（3,362 銘柄、
書類種別の5位）**、うち **10,805 件（78%）が決算と別の日**に出ていた。

**「無い」は「見えなかった」と区別が付かない。** 取りこぼしのある経路で「無い」
を結論にしてはいけなかった。

## 出典

**持っていない。** 「上方修正した銘柄は上がり続ける」という形で語られるのを
見かけた、という以上のものがない（既存8本と同じ）。

**あとから論文を探して埋めない**（`docs/PURPOSE.md`）。空欄のほうが正直である。

## 事前確率は低い。そう書いておく

**#2・#3（決算後ドリフト）は両方とも不合格だった。** 予想修正のドリフトは
**同じ「ニュースへの過小反応」という族**である。

違うところは2つ。**日付集合が違う**（決算と同じ日の修正は §2 で外す）ことと、
**情報の種類が違う**（実績ではなく、会社が出す将来の見通し）こと。

**それでも、2度落ちた族の3本目である。** 通らなかったときに「やはり」と読める
ように、**先に書いておく。**

## データは二度と増えない

`fins/summary` の一括は **2021-09 〜 2026-09 の5年で固定**である。J-Quants を
Free に落としたので増えない。EDINET の有価証券報告書に予想修正は無い。

**OOS は約2.5年で、それが最終である。** 足りなければ、足りるようになる日は
来ない。

---

## §0. 検出可能性ゲート（**最初に埋める。ここで落ちたら封印しない**）

### 封印しない線 — **測る前に置く**

> **IS の1イベントあたり平均超過リターンの片側95%下限が 1.2% を下回ったら、
> 封印しない。**

| | 値 | 出どころ |
|---|---|---|
| 往復費用 | **0.4%** | `reversal.COST_ROUND_TRIP`（ロングオンリー・20営業日） |
| しきい値 | **1.2%** | その**3倍** |

**#8 と同じ規則・同じ理由である。** 費用を超えるだけの下限を置くと、#7 が
入った帯（取れ高が費用を超えていながら検出できる大きさに届かない）にまっすぐ
入る。**帯に入った判定は「無い」と「あると言い切れない」を分けられないまま、
判定を1回消費する。**

**この線は、推定値を見た後に下げない。** 下げたくなったら、それは「設計を
変える」であって「しきい値を直す」ではない。

### 見込みをどこから取るか — **段2（自分の IS から推定する）**

**段1は使えない。** この設計で報告された文献値を持っていない。

段2の条件3つ。

1. **上の「1.2%」を、測る前に書いてコミットした。** ✅
2. **IS と OOS は1日も重ねない。** §6 で日付を固定する ✅
3. **検出できる差は OOS のイベント数で計算する。** 全期間ではない ✅

### 埋めるもの

**件数は `research\上方修正の件数.bat`、散らばりは `research\上方修正の散らばり.bat`
が出す。** 前者は**リターンを1つも計算しない。**

| 項目 | 値 | どこから取るか |
|---|---|---|
| 上方修正イベント（全期間） | **未測定** | 一括の `fins/summary` から |
| うち決算と同じ日（**外す**） | **未測定** | §2 |
| 流動性の下限を通した後 | **未測定** | §2 |
| イベント数（IS / OOS） | **未測定** | 境は原本が覆う期間の真ん中 |
| 上位1割の日の占有 | **未測定** | 固まっていれば膨張が大きくなる |
| 1イベントあたりのSD | **未測定** | IS から。**判定に使う期間は見ない** |
| 重なりの膨張 | **未測定** | 実測。**20営業日の窓が重なるので 1.0 にはならない** |
| 見込み（片側95%の下限） | **未測定** | 段2 |

### 判定に使うコマンド

```
uv run stock-ai power-gate --sd <SD> --periods <OOSのイベント数> \
  --low <下限> --high <上限> --inflation <膨張> --budget 20
```

**合格線は1つだけ。見込みの下限が、検出できる差を上回ること。**

通らなかったら「検出力不足」で終わらせず、**何イベントあれば足りるか**を書く。
**ただし、この説では年数に直しても意味が薄い**——データは増えないからである。

---

## §1. 仮説

**会社が業績予想を上方修正した後、株価は上がり続ける。**

### 逆の符号が出ても「発見」にしない

**差が有意に負（＝上方修正の後に下がる）だったとき、それはこの説の不合格で
ある。** そう書いておかないと、**両側で当たりを狙う釣り**になる（#9 で同じ形を
禁じ、#8 で実際に拾いたくなった）。

## §2. ユニバース

| | 決めたこと | 理由 |
|---|---|---|
| 市場 | 東証（TOKYO PRO Market を除く） | 買えない銘柄を入れない |
| **決算と同じ日の修正** | **外す** | **#2・#3 と同じ日付集合を使わない。再開の前提そのもの** |
| 流動性の下限 | 売買代金 1億円／日（`pead.MIN_TURNOVER`） | 既存の登録と揃える |
| 貸借区分 | **問わない** | ロングのみなので空売りが要らない（#8 はここで 70% 落ちた） |
| 外国会社の株式 | **入らない** | 2026-09-16 に universe から外した |
| 価格系列の不連続 | 1営業日 ±50% を超える動きは除く | 既存と同じ |
| 上場廃止銘柄 | **含む** | 生存バイアスを入れない |

## §3. イベントの定義

> **`DocType == EarnForecastRevision` の開示のうち、会社予想が前回比
> **+5% 以上**に変わったもの。**

- 比べるのは**同じ会計年度の**通期予想。年度をまたいだ比較は「修正」ではない
- 見る科目は **`net_income`**（`forecast_revision.find_revisions` の既定）
- +5% の出どころは `forecast_revision.DEFAULT_MIN_CHANGE`。**この登録のために
  決めた数字ではなく、既にコードにある値を引く**

### 並べ替えない

**修正幅で分位に割らない。** 二値の出来事として等加重で持つ。

**修正幅で並べるなら、それは別の1本である。** 割り方が1つの自由度になり、
**#3（SUE）はまさにその形で落ちている。**

## §4. 売買ルール

| | 決めたこと |
|---|---|
| イベント日 D | **開示日**（`disclosed_on`） |
| エントリー | **D+1 営業日の始値** |
| エグジット | **D+20 営業日の終値** |
| 建玉 | 1イベント1単位、等加重。**ロングのみ** |
| 費用 | 往復 0.4%（`reversal.COST_ROUND_TRIP`） |

### D の終値では入らない

**開示時刻に賭けない。** 予想修正は引け後に出ることが多いが、**多いことと
全部であることは別である。** D+1 の寄付きなら、いつ出ていても取れる。

### 窓を 20営業日にする理由

**#3・#6 と揃える。** リターンを見て決めていない。

**#8 のように機構から窓が出る説ではない**ので、既存と揃えるのが**いちばん
自由度の少ない決め方**である。

## §5. 評価指標

**主要指標：上方修正イベントの、市場対比の平均リターン（D+1 始値 → D+20 終値、
費用差引後）。符号は正を期待する。**

**主要指標は1つだけである。**

### 副次指標（**判定には使わない**）

| | 何が分かるか |
|---|---|
| 下方修正の同じ測り方 | 逆向きに出るか。**出なくても上方側の判定は変わらない** |
| D−5 〜 D の値動き | **開示より前に動いているか** |
| 決算と同じ日の修正の同じ測り方 | 外した集合が違う振る舞いをするか |
| 修正幅で分けた内訳 | **並べ替えの設計に使わない。** 見るだけ |

**副次を判定に使わない。** 使えば基準が2つ以上になり、多重検定になる。

## §6. 期間分割

| | 期間 | 用途 |
|---|---|---|
| 推定（IS） | 原本が覆う期間の**前半** | 分散・見込みのみ |
| **判定（OOS）** | **後半** | **一度だけ** |

**1日も重ねない。** `end` は「この日より後のデータを1つも使わない」という意味で
実装する。**D+20 が IS の外に出るイベントは、D が中にあっても使わない。**

## §7. 生存バイアス

日付ごとの名簿から、その日に上場していた銘柄を取る。**廃止銘柄を落とさない。**

## §8. 先読みバイアスのチェックリスト

- [ ] イベント日を `disclosed_on` に置いているか（**受付日や適用日ではない**）
- [ ] D の終値で入っていないか
- [ ] 前回予想が、その開示より前のものか
- [ ] 名簿がその日時点のものか

## §9. 主張できないこと

- 米国株については何も言わない
- **配当予想の修正については何も言わない。** 業績予想だけである
- **下方修正については何も言わない**（副次で見るだけ）
- **修正幅の大小については何も言わない。** 並べ替えていない
- 2021-09 より前については何も言わない

## §10. 中止条件（**判定を回さずに閉じる**）

- IS の片側95%下限が **1.2% を下回ったら封印しない**（§0）
- §0 のゲートが通らなければ封印しない
- 流動性を通した後のイベントが **OOS で 1,000 件を割ったら**、設計を見直す
- **上方修正の判定に下方修正の結果を使いたくなったら封印しない**（§5）

## §11. 合格基準と読み方の表（**結果を見る前に確定させる**）

**測ってから書く。** SD が出ないと検出できる差が決まらない。**§0 を通ってから、
封印の前にここを埋める。**

## §12. 合格したときに何をするか（**封印前に決める**）

**測ってから書く。**

## §13. 判定（回した後に書く）

**まだ回していない。**

## §14. 記録

| 日付 | 何を決めたか |
|---|---|
| 2026-09-03 | 閉じた（**理由は事実として誤りだった**） |
| 2026-09-08 | 訂正。再設計できる状態に戻した |
| 2026-09-16 | 再開。**上方のみ・二値**、**決算と同じ日は外す**（ユーザーの判断） |
| 2026-09-16 | **封印しない線を 1イベント 1.2%**（往復費用の3倍）と決めてコミット |
