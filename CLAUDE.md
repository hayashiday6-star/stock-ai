# stock-ai

JP/US 株式のスクリーニング・バックテスト・監視システム。Python 3.13 / `uv` / typer / SQLAlchemy 2.0 / pydantic-settings。

利用者は Windows で `.bat` をダブルクリックして使う。コンソール上で `uv run` を直接叩くことは基本的にない。

## 何のためか

**`docs/PURPOSE.md` を先に読むこと。** この文書は「どうやるか」しか書いていない。

一言で言えば、**世間で語られる投資の定説・アノマリー・都市伝説を、事前登録つき
で検証し、合格した説だけを自動売買に載せる。不合格の説も検証過程ごと公開する。**

迷ったときはそこに帰る。ここに書いてある規則は全部、**「もっともらしいが違う値
が黙って出る」ことを防ぐため**にあり、それは判定を信じられるものにするためで
ある。

**実装前に計画を提示し、承認を得てから書く。**（`docs/PURPOSE.md`「作業の進め方」）

## 開発ブランチ

作業は `claude/recent-activity-z1t0is` で行う。**このセッションだけで2回、別ブランチ（`main` や無関係な作業ブランチ）に乗ったまま作業しかけたことがある。** 何かコミットする前に `git branch --show-current` で確認すること。

まとまった区切りが付いたら `main` への取り込みは直接マージではなく Pull Request で行う（ユーザーの明示的な希望）。

## セットアップ・検証

```
uv sync --extra data --extra db --extra ai  # 必要な extra は都度足す。--extra は追加ではなく置き換え
uv run pytest -q
uv run ruff check .
uv run ruff format --check .
```

コミット前に上記3つを必ず通す。CIも同じ3つを見る。

`per-file-ignores`（`pyproject.toml`）で日本語docstring由来の `D415` を個別ファイル無視している。新しいファイルで同じ理由の警告が出たら、そこに追記する。

## Windows の `.bat` / `.ps1`

- **`.bat` は純ASCII。** cmd.exe はコンソールのコードページ（日本語Windowsではcp932）で読むため、UTF-8の日本語はmojibakeになり、mojibakeのバイト列がecho途中で行を終わらせてその後をコマンドとして実行してしまうことがある。日本語は `.ps1` 側に書く。
- **日本語を含む `.ps1` は UTF-8 with BOM** で保存する。
- `.gitattributes` で `*.bat`/`*.cmd` は `eol=crlf`、それ以外は `eol=lf`。
- 新しい `.bat` を追加しても、pull するまでユーザーの手元には存在しない。**`.bat` は自分の存在を知らせられない** ので、`0-最新にする.bat`（`scripts/0-update.ps1`）が増えたファイルと減ったファイルを一覧で出す。ユーザーには常にこれを先に実行してもらう。
- **`.bat` の置き場所は3つ。** 直下は日常的に使うものだけ（0〜6 と APIキー設定・PowerShellを開く・ダッシュボード起動）。`checks/` は接続やデータの一度きりの確認。`research/` は説ごとの検証で、`docs/HYPOTHESES.md` の項目と対応する。
- **サブフォルダの `.bat` は `cd /d "%~dp0.."` と `"%~dp0..\scripts\...ps1"` を使う。** 直下用のパスのまま移すと動かない。
- `.bat` の ASCII 検査（`tests/test_encoding.py`）は再帰的に見ている。**直下だけを見る glob に戻すと、サブフォルダの `.bat` が黙って検査対象から外れる。**

## 秘密情報

- `.env` は git-ignore 済み。API キー・トークンは `APIキー設定.bat`（`scripts/set-key.ps1`）経由で設定する。このスクリプトは入力を隠し、`ValidateSet` に載っているキー（`EDINET_API_KEY` / `JQUANTS_API_KEY` / `ANTHROPIC_API_KEY` など）専用。
- **`JP_PRICE_SOURCE` や `JP_STATEMENT_SOURCE` のような設定値（秘密ではない）はこのツールでは扱えない。** `.env` を直接 `notepad .env` などで編集する。過去に両者を混同して、設定値を書くつもりが `EDINET_API_KEY` を上書きしてしまったことがある。
- ログや例外メッセージに秘密情報の生値を出さない。長さとSHA-256指紋（先頭のみ）で「値が変わったか」を確認できるようにする（`uv run stock-ai info` の表示方式）。
- **立花関係のファイルは `tachibana/` にまとめる**（`private.pem` / `public.txt` /
  `session.json` / `history.json` / `master.json`）。README 以外は gitignore 済み、
  秘密鍵はPOSIXで0600、例外メッセージに内容を含めない。
- **秘密鍵を作り直させない。** 作り直すと立花に登録済みの公開鍵と合わなくなり、
  登録からやり直しになる。`resolve_path` が旧い置き場所（直下の
  `tachibana_private.pem`）も読み、鍵の作成コマンドは旧い場所に鍵があれば拒否する。

## 日本株データソース

J-Quants の有料プランを解約する方向で移行中（`docs/TACHIBANA.md`、`docs/EDINET.md` に詳細）。

- 価格: `JP_PRICE_SOURCE`（`jquants` | `tachibana`）
- 財務諸表: `JP_STATEMENT_SOURCE`（`jquants` | `edinet`）

どちらも `uv run stock-ai info` の出力に必ず表示すること — 切り替えたつもりで切り替わっていないことに、数字が変わらないという形でしか気付けなくなるため。

新しいデータ取得経路を追加するときは、`bulk-fetch` のような一括コマンドが実際にその設定を見ているか確認する。`BulkIngester` はプロバイダを差し替え可能な作りだが、CLI側が新しい設定を配線し忘れて、切り替えたはずが常に旧経路を叩き続けていた実例が複数ある。

## 合格に要るリターンと、合格の条件

`docs/PASSING.md`。**生成物である**——`uv run stock-ai passing --write
docs/PASSING.md`（`research\合格の条件.bat`）が作り直す。**手で直さない。**

**数字を書き写していない。** 線が変われば要るリターンも全部変わる。実際
2026-09-17 に 3.02 → 3.39 に動いた。**書き写した数字は、古いまま
もっともらしく見え続ける。**

**新しく散らばりを測ったら `backtest/passing.py` の `SHAPES` に足す。**
そのとき**どの事前登録に書いてあるか**も一緒に置く（出典の無い数字を書かない）。

## 検証した説の一覧

`docs/HYPOTHESES.md` が、これまで試した説とその結末の一覧である。

**説の状態が変わったら、事前登録と一覧の両方を直す。** 片方だけ直すと、
どちらが本当か分からなくなる。具体的には次のとき。

- 事前登録を封印したとき（状態を「検証中」にする）
- 判定が出たとき（合格・不合格・検証不能とその理由）
- 説を諦めたとき、設計をやり直すと決めたとき

閉じた理由は**専門用語なしで**書く。半年後に読んで分かることを基準にする。
数字は事前登録から引き写し、一覧の中で計算し直さない（二重管理になる）。

**出典の無い数字を書かない。** 会話に出ただけで文書に記録していない値は、
そう明記するか、測り直してから書く。

## まだ登録していない説

`docs/CANDIDATES.md` に順番つきで置いてある。**ここに載ることには費用が
掛からない**——`docs/HYPOTHESES.md` に載った時点で予算を1本使う。

**登録したら候補の表から消す。** 2箇所に同じ説があると、どちらが本当か
分からなくなる。`tests/test_candidates.py` が消し忘れを止める。

**順番は約束ではない。** 上から §0 を当て、通らなければ次に行く。
**§0 で閉じても判定は減らない**ので、そこを速く回すほうが得である。

### 同じ式を3つ書けば、1つは間違える（**重なりの膨張を落とした**）

標準誤差 `SD × 膨張 ÷ √n` が、`PowerEstimate`・`passing.Shape`・`wall.Wall`
の3箇所に別々に書かれていた。**3つ目だけが膨張を掛けていなかった**
（2026-09-19）。

**20日保有・毎日エントリーなら膨張は理屈の上で4倍前後**になる。落とせば
**壁が数倍低く出る**——また緩む向きである。

`power.standard_error` / `power.detectable_difference` に1つだけ置き、3つとも
そこを呼ぶようにした。`tests/test_power.py` が、**3つが同じ答えを返すこと**と
**`periods_needed` と往復すること**を見ている。

**「同じ処理を2つ書かない」は、式にも当てはまる。** 式は短いので、つい
書いてしまう。**短いものほど、違いに気付けない。**

### イベントの定義そのものが、不連続を拾っていないか

壁の下見を1回目回したら、**落ちるナイフの SD が 273%／イベント日**と出た
（2026-09-19）。20営業日・約50銘柄の等加重バスケットのばらつきとして
**ありえない。**

原因は #6 で確定済みのもの——**分割・併合の調整漏れ**である。そして今回は
もっと悪い。**「5営業日で −20%」も「前日終値から −3% の窓」も、調整漏れの
分割がまさにそう見える形**だった。**窓に混ざるのではなく、イベントの定義が
不連続を拾っていた。**

**同じ4行が5箇所に書かれていた**（`reversal` / `lowvol` / `lowvol_census` /
`factor_panel`）ので、`backtest/discontinuity.py` に1つだけ置いて全部そこを
呼ぶようにした。`tests/test_discontinuity.py` が**6箇所目を止める。**

**新しくイベントを定義するときは、その定義が不連続を拾わないかを先に見る。**

### 事前登録を書く前に、壁の高さを見る

**7本続けて §0 で閉じた。** 説が悪いからではなく、**設計の散らばりと観測数で
壁の高さが決まっている**——月次の分位ロングショートはどれも年20%台を要求する
（`docs/PASSING.md`）。

`research\壁の下見.bat`（`wall-survey`）が、**まだ登録していない説について
検出できる差だけ**を出す（`docs/WALL.md`。生成物）。

**効果を1つも計算しない。** 検出できる差は `線 × SD ÷ √n` で、散らばりと
観測数だけで決まるので、これを見ても答えを先に見たことにならない。
**`Wall` に平均の欄を作らない**——`PowerEstimate` と同じ理由で、入れられる形に
すると「効果がありそうだから通す」が書ける。`tests/test_wall.py` が、平均を
計算していないことを機械的に見ている。

**壁を比べても、どの説が正しいかは分からない。** 選んでいるのは検出力だけで
ある。#10 が封印できなくなったのは**効果を見て設計を選べる形**だったからで、
そことは別である。

## 新しい説を検証するとき

**`docs/PREREG_TEMPLATE.md` を写して埋める。§0 を埋める前に、それ以外を
書き始めない。**

§0 は検出可能性ゲートである。`uv run stock-ai power-gate` が当てはめる。
**合格線は「見込みの下限が、検出できる差を上回ること」の1つだけ。**

中央値ではない。#7 は封印前に「五分五分」と気付いていて、そのうえで回して
負けた側に落ちた。**気付くだけでは足りなかった。止める場所が無かったからで
ある。** ゲートがその場所になる。

通らなかったら「検出力不足」で終わらせず、**何期あれば足りるか**を書く。
年数にすれば、手元の年数と引き算ができる。

判定は一度きりの資源である。**通る見込みの薄い設計に使わない。**

## 出力を小さく保つ（会話に貼られる前提で作る）

**このプロジェクトの検証は、ユーザーが `.bat` を実行して出力を貼る形で進む。**
出力の長さは、そのまま毎回のトークン消費になる。長い出力は高いだけでなく、
**信号が埋もれる**——実際、署名付きURLの400字が4行続いた回がある。

- **1リクエスト1行の記録をコンソールに出さない。** `httpx` などは
  `ConsoleNoiseFilter` でコンソールからだけ落とす。**ファイルには残す**ので、
  後から追う手段は減らない。診断したいときは `LOG_LEVEL=DEBUG`。
- **`pytest` は既定で網羅率の表を出さない**（測りはする）。約100行が毎回減る。
  見たいときは `uv run pytest --cov-report=term-missing`。
- **新しい `.ps1` は、結果の表と警告だけを出す。** 説明の散文は docs に置き、
  実行のたびに刷らない。「なぜこれを実行するのか」は `.bat` の冒頭に数行で足りる。
- **進捗表示は1行に収める。** 途中経過を残す形にすると、貼ったときに何十行にもなる。
- 貼ってもらうときは「表と警告だけで足りる」と伝える。`INFO HTTP Request` の
  行が答えだったことは一度も無い。

減らしてよいのは**繰り返される定型**であって、判断の材料ではない。段ごとの
件数・警告・不一致は、むしろ増やす側に倒す。

## 自分の粗さを、注意ではなく仕組みで止める

**「気を付ける」は3度とも効かなかった。** 気付いたのはいつも数字を見たあとで、
書いている最中ではない。だから注意書きを増やさず、**落ちるものを置く。**

### `.bat` が引数を2つ以上固定したら、その組み合わせを動かすテストが要る

`tests/test_launchers.py` が `.bat` を全部読み、固定している引数を数える。
2つ以上あるものは `COMBINATIONS_EXERCISED` に**テスト名ごと**登録しないと
落ちる。登録するには、先にそのテストを書くことになる。

片方ずつなら正しく動くのに、**一緒に渡したときだけ後から来たほうが消える**、
という形がある（`-NoShapes` が `--columns` を消した）。`.bat` を書いたのも
CLI を書いたのも同じ側なのに噛み合わなかった。**両方を書けることは、両方を
試したことではない。**

### 見張りそのものが、黙って死んでいないか

SessionStart フック（`.claude/hooks/session-start-branch-check.sh`）が
**一度も鳴っていなかった**（2026-09-19 に発覚）。末尾で `jq` を呼んでいて、
**ユーザーの Windows に `jq` が無い。** `set -eu` の下で exit 127、**出力なし。**

`main` で直接作業する・push 忘れが残る——**「3回起きている」と上に書いてある
事故を止めるために置いたフックである。** いつから鳴っていなかったかは分からない。

**こちらの手元では再現しなかった。** cloud には `jq` が在る。**「手元で緑」を
根拠に「直っている」と読まない**——`--help` の幅と同じ形である。

3つ置いた。

- **依存を消した。** `printf` で JSON を組む。呼ぶ外部コマンドは `git` だけ
- **黙って終わらない。** `trap` を置き、途中で落ちたらその旨を出す。`git` が
  無いことも1行で言う（**リポジトリの外に居ることとは別**。そちらは黙ってよい）
- **`tests/test_hooks.py`** が見る。**関門は `uv run pytest -q` 1本**なので、
  そこに入れれば忘れられない

**いちばん効くテストは、シェルを使わないほうである。** `sh` が要るテストは
Windows で skip され、**skip は緑に見える**——壊れていたのは、その skip される
側の環境だった。だから「**外部コマンドの許可リスト**」をファイルを読むだけの
検査として別に置いた。`jq` を足し戻せば、どの OS でも落ちる。

**枝名は外から来る値である。** `"` は git の枝名として通る（`\` は git が
弾く）。固定の文面から `"` を抜くだけでは足りず、**枝名にそれが入れば同じ
「黙って死ぬ」が起きる。** そこだけエスケープしている。

### 関数の中の `import` は、通らないと落ちない

`cli._universe_calendar` が
`from stock_ai.backtest.universe_benchmark import equal_weighted_daily`
と書いていて、**その名前は存在しなかった**（2026-09-19）。書いたつもりで
書いていない——2つを1つのスクリプトで足そうとして、**最初の assert が落ちて
中断し、片方だけ当て直した。**

気付いたのは**ユーザーの PC で `--universe` を付けた実行が落ちたとき**である。

- `tests/test_turn_of_month.py` は 332行あったが、**その枝を一度も通って
  いなかった**
- こちらの疎通確認は**空の DB** で叩いたので、「1306 の価格が無い」で
  手前で終わり、**到達していなかった**
- `import` が関数の中なので、**モジュールを import するだけでは捕まらない**

**`factor_panel` と同じ形である。** その間テストは全部緑だった。

2つ置いた。**経路ごとに end-to-end を足す方式は、次の1本を書き忘れた瞬間に
同じことが起きる**——現にそうなった。

- **`tests/test_deferred_imports.py`** が `src/` 全体の関数内 import を
  機械的に拾い、`stock_ai.*` の名前が実在するかを確かめる。**新しい経路を
  足しても、登録も追加も要らずに守られる**
- **本物のコマンドを、中身の入った DB で1本通す。** 空の DB で叩いて手前で
  終わるのを「疎通した」と読まない

### テストの足場に、定数の足を置かない

上の end-to-end が `typer.Exit` で落ちた。原因はコードではなく fixture で、
**`1306` の足が定数だった**——散らばりが 0 なら標準誤差も 0 で、対照は
「1回も測れなかった」で終わる。

**足場のせいで落ちると、原因を探す時間が要る。** 価格の fixture は
乱数歩行にする。

### 直しが、別のものを壊していないか

対照の汚染を `exclude`（本物の窓を外す）で直したら、**偽の観測が作れなく
なった。** 偽の窓が本物の窓を挟むと、「窓の外」が**本物の窓だけになり、
除外して空になる。**

そして空にならない回も、**窓の外の長さが 4〜22日 とばらついていた**
（本物は常に約16日）。**同じ推定量を測っていることにならない。**

`outside_pool` で、窓の外を**その隙間のふつうの日に限った。**

**直したあと、直した先で何が変わったかも見る。** ここは「落ちた」ので
気付いたが、**落ちずに数字だけ変わる形もありうる。**

### 対照が、本物の効果に汚染されていないか

#13（月替わり）の対照を1回目回したら、`t` の平均が **+0.40** 出た
（2026-09-19）。SD は 1.02 で素直だったのに、中心がずれていた。

原因は**対照の作り方**だった。偽の窓は本物の窓を避けて置かれるので、
偽の「窓の外」＝`前の偽窓の終わり+1 〜 今回の偽窓の始まり-1` は、
**構成上かならず本物の窓をまたぐ。** 本物の効果が**引き算する側**に混ざる。

**これは推測ではなく、置き方から証明できる。** そして実データでも、本物の窓に
だけ効果を仕込んだ盤面で偽の観測が 0 から離れることを確かめた。

**しかも、混ざる向きから本物の符号が逆算できてしまう。** §0 の前に答えを
見ることになるので、**気付いた時点で計算を止めた。**

直し方は、**本物の窓を偽の窓からも窓の外からも外す**こと。対照は「ふつうの日」
だけを見る。**片方だけ外すと、外した日が「無かったこと」ではなく「相手側に
寄った」ことになる。**

**イベント型の +0.49 と同じ形に見えたが、原因は違った。** あちらは加重の
違い（データ側）、こちらは対照の作り（こちら側）。**「前と同じだろう」で
済ませない。**

### `t` だけ見ない。下にある量も一緒に出す

イベント型の +0.49 は `t` しか出していなかったので、分解に2手かかった。
**`t` は比なので、分子が動いたのか分母が動いたのかが分からない。**

対照には**下にある量（1観測あたりの差）も出す**ようにした。次に中心がずれた
とき、1回で分けられる。

### §0 の当てはめを、呼ぶ側で書き直さない

#14 の `january-power` が `power.gate` を呼ばず、**同じ判定を書き直していた**
（2026-09-19）。しかも**見込みの下限ではなく上限**と検出できる差を比べていて、
**「見込みが検出できる差をまたいでいる」を通す**形になっていた。

**#7 が落ちたのはその形である。** 五分五分と気付いたうえで回して、負けた側に
落ちた。ゲートはそれを止めるために在る。

**合格線は「見込みの下限が、検出できる差を上回ること」の1つだけ。**
上限は「上限すら届かないなら望みが無い」と言うためだけに使う。

`docs/PASSING.md` が月次の線を全部の形に当てていた件、`power-gate` が校正前の
3.02 を使っていた件と**同じ型**である。**判定の当てはめが2箇所にあると、
片方が緩む。** しかも緩むほうは、**「通った」と出るので気付きにくい。**

### `t` の SD から線を引いてよいのは、観測が多いときだけ

管ごとの線は、陰性対照で測った **`t` の SD** を正規分布の分位点に掛けて
作ってある。**それが成り立つのは自由度が大きいときだけ**である。

#14（1月効果）は**年に1回しか観測できない**ので n=9、自由度 8 になる。

| 自由度 | 正しい線 | SD 方式 | ずれ |
|---|---|---|---|
| **8** | **4.33** | 3.49 | **SD 方式が 24% 甘い** |
| 103 | 3.10 | 3.05 | 1.6% |

**緩める向きの取り違えである**——線が低ければ検出できる差も小さく出るので、
**通ってはいけない設計が §0 を通る。** `power-gate` が校正前の 3.02 を使って
いたのと同じ形である。

**既存の管を遡って直す話ではない。** 自由度 100 以上なら両者は一致する。
`multiplicity.student_t_line` が正しい値を出す（閉じた式。数値積分ではない
——刻みを間違えても**答えらしきものが返る**ため）。

**観測が少ない管では、対照の SD ではなく対照の裾の分位点から線を取る。**
400回では 0.25% の裾に1回しか入らないので、回数のほうを増やす。

### ループの中の1行記録を、モジュールごと黙らせない

対照が `turn_of_month.build_series` を400回呼び、出力が **112KB** になった
（2026-09-19）。

`_QUIET_ON_CONSOLE` は `httpx` のような「いつ何回出ても読む価値が無い」
ライブラリの固定リストである。**こちら側の部品をそこに入れると、1回だけ
回すときの件数や警告まで消える。**

要らないのは**ループの中で同じ行が400回出るとき**だけなので、
**呼ぶ側が一時的に黙らせる**（`logging.quiet_on_console`）。**ファイルには
残る。**

### 走らなかった回は、出力に出ない

日次自動化が4日ぶん走っていなかった（2026-09-19 にユーザーが `logs/` を手で
数えて発覚）。`logs/daily` に 4日、`logs/accumulation` に 2日の穴。

**「土日だから」ではない。** 09-12(土)・09-13(日)・09-19(土) は走っている。
**09-16 は同じ日の 07:02 に accumulation が走っている**ので PC は動いていた。
06:00 の daily だけがログを1行も残していない。

`Get-ScheduledTaskInfo` は両方 `LastTaskResult: 0` と出る。**それは「最後に
走った回」の話で、走らなかった回は数に入らない。** 走らなければ結果も残らない
ので、**「異常なし」の顔をしたまま抜ける。**

**無いことは、出力に出ない。** 出すには、**在るべき日を先に決めて引き算する**
しかない。`uv run stock-ai info`（`2-動作確認.bat`）が数える
（`core/log_gaps.py`）。**`daily` と `accumulation` は独立に数える**——片方が
埋まっていると、もう片方の穴が「異常なし」に化ける。

### 常に点く旗は、何も区別しない

`uv run stock-ai info` の版の行が、**いつも `+変更あり`** を出していた
（2026-09-19 に発覚）。`git status --porcelain` を**未追跡込み**で見ていて、
`data/valuation_monthly.csv.gz`（恒常的に未追跡の生成物）が必ず引っかかる。
**コードを一行も触っていなくても点く。**

「広すぎる幅は、狭すぎる幅より悪い」の逆向きである——**常に赤なので、誰も
見なくなる。** どちらも「確かめたつもりになる」ところは同じ。

**`--untracked-files=no` にはしなかった。** それだと `src/` に増えた未追跡の
`.py` ——**本物のコード変更**——まで見逃す。**見る範囲のほうを絞った**
（`version.CODE_PATHS`）。

そして**札も変えた**（`+コード変更あり`）。**意味を狭めたのに札が同じだと、
「きれい」と出たときに作業ツリー全体がきれいだと読まれる。**

**テストは両向きに置く。** 「未追跡だけ → 点かない」と「追跡ファイルを変更
→ 点く」。片方だけだと、**常に点かない旗に倒しても緑になる。**

### 突き合わせは `overlap.overlapping` を通す

**重なる期間だけを見る**を、同じ形で3度踏んだ。片方が始まる前を「欠け」と
呼んでしまう。`Overlap` は範囲の外を**捨てずに持つ**ので、「比べていない」が
「無い」に見えなくなる。

### `--help` の描画結果に賭けない

端末の幅で折り返す。幅が足りないと綴りが途中で割れ、**手元で緑・CI で赤**に
なる（2026-09-15、PR #100。幅40で再現した）。引数が在るかは
`declared_options()` に聞く。`tests/test_cli.py` の
`TestNoTestBetsOnRenderedHelp` が、この形が戻るのを止める。

**手元の3つを通したことは、CI を通したことではない。** 幅のように、こちらの
手元にしか無い条件がある。疑わしいときは `COLUMNS=40 uv run pytest -q`。

### fixture で確かめた前提を、実データの規模でも確かめる

`identity_check` は 1,588万行で落ちた。`pbr` が 0 なら、`bps` がどれだけ
大きくても積は 0 になる。**コメントに「片方が 0 になる行は判定から外れて
いるはず」と書いてあった。書いたが、確かめていなかった。**

fixture は数行なので、その形が1件も入らない。**書いた前提は、成り立つことを
確かめるか、前提が要らない形に変える。** ここは積そのものに床を当てて、
前提を消した。

### 警告は、見ている列を絞らない（**2度目。2026-09-16**）

表に出ていることと、目に入ることは別である。`eps` が3年ぶん皆無だったのに
警告が1行も出なかった——見ていたのが `market_cap` だけだったため。**表は
読む側が気付く必要がある。気付かなくても目に入るのが警告である。**

**同じ形を #5 で踏んだ。** 鍵の名前が違ったときに一覧を出す警告を書いたが、
**会社予想の列にしか付けていなかった。** 予想は読めたので警告は鳴らず、
**会計年度末が 72,156 件すべてで読めていない**ことは表の1行にしか出なかった。

**「1列だけ守る警告」は、その列が通ったときに黙る。** 必要な列すべてに同じ形で
当てる。

**そして、0 を「読めた」と読まない。** 同じ回の表で「会社予想が読めなかった 0」
と出ていたのを根拠に「予想は全件読めた」と報告した。**実際は予想も読めていな
かった**——年度末の検査が先に `continue` していて、予想のカウンタが**一度も
動いていなかった。** 検査を直列に並べると、手前が全部を弾いたとき、後ろの数字は
0 のまま「異常なし」の顔をする。**列ごとに独立に数える。**

そして**読み口が捨てている列は、読み口からは見えない。** `FS` の中の鍵をいくら
並べても、探しているものが `FS` の外にあれば答えにならない。**原本そのものの
列名も出す。**

### このリポジトリで `git reset --hard` を勧めない

`data/universe_snapshots` は **git 管理下にある。** 名簿の取り直しをすると
作業ツリーは当たり前に汚れ、しかもその中身は **J-Quants のプランを落とすと
二度と取れない。**

2026-09-15 に「clean なら `reset --hard`」と案内した。clean ではなく、
**取り直した名簿62枚と、Premium でしか取れない約170枚**が入っていた。
条件を付けたから助かっただけで、条件を付けた理由は「念のため」だった。

**このリポジトリでは、作業ツリーが汚れているのが通常である。** 退避より先に
記録する（`checks\名簿を記録する.bat`）。

### ユーザーが作業中の枝を force-push しない

同じ日に、`main` へ squash 済みの枝を force-push した。履歴としては安全
だった（中身は取り込み済みと同一）が、**ユーザーの手元がその枝に乗っていた。**
早送りできなくなり、復旧の手順を踏ませることになった。

**取り込み済みかどうかだけで判断しない。** その枝を誰かが開いているかを先に
考える。

### 落ちたものは、直す前に手元で落とす

`identity_check` の同じ行で**2度落ちた。** 1度目の直しは `pd.NA` を疑って
消したが、**それは原因ではなかった。** もっともらしい筋を1つ思い付いて、
確かめずに直して、押した。

2度目は先に再現させた。原因は別だった——**1ファイルの中である列が1件も
埋まっていないと object 型になり、`concat` で他のファイルの float 列まで
引きずられる。** 2008年の `EPS` がそれである。

直したあと、**直す前のコードでそのテストが落ちることも確かめる。** 落ちない
なら、そのテストは何も守っていない。

### 読み口は、1ファイルではなく**連結**まで通す

fixture を1ファイル分しか作っていなかったので、`concat` を一度も通して
いなかった。**「部品だけでなく、組み立てを1本通す」は既に下に書いてある
規則である。** 書いてあるのに適用しなかった。

読み口に「1本読む」と「束ねる」があるなら、**束ねるほうにもテストを置く。**
ファイルごとに形が違いうるデータでは、そこにしか出ない不具合がある。

### 出した数字を、自分で見る

株式数の中央値を「桁が妥当かを見る」ために出した。**出したまま、検査に
使わなかった。** 実データで **21 株**と出ているのに、「時価総額は終値と同じ
尺度で作られている」と緑を出した（2026-09-15）。日本の上場企業に 21 株の
会社は無い。

**比例していることと、単位が円であることは別である。** 定数倍は散らばりを
変えないので、比例の検査は通ってしまう。`units_hold` を別に置いた。

`MktCap` は百万円単位だった。**円として使えば、百万倍間違える。** このプロ
ジェクトが繰り返している「もっともらしいが違う値が黙って出る」形そのもの。

そして**「何倍ずれているか」を1つの数で言わない。** 範囲の中心から逆算すると
根拠の無い数字になる（最初そうした）。下限より何桁下かは測れる。**どの単位
なのかを決めるのは、その数字を見た人である。**

### 検査に出てこない列を、確かめたつもりにしない

`PER × EPS` と `PBR × BPS` の突き合わせは 871万行で食い違いゼロだった。
**そこに `market_cap` は1度も出てこない。** 見ているのは4列だけである。
そして欲しかったのは時価総額のほうだった。

株式数は原本に無いので `時価総額 ÷ 終値` で割り出し、**段差**（分割・増資）
と**水準**（月ごとの散らばり）の両方を見る。片方だけでは足りない——時価総額
が終値に連動していないと、1日あたりは丸めより小さく動くので**段差では
捕まらない。** その形を作って、一度素通りさせた（2026-09-15）。

### 全体の数字で、行ごとの無意味さを隠さない

**同じ形を3度踏んだ。**

幅の中央値が 0.55% で「ずれの典型と同じ桁にある」と緑を出した。ところが
`EPS` が 0.01 の行では `PER` が 37,230 になり、`EPS` の ±0.005 が終値の
**±36%** に化ける。その行は 31% ずれていたのに「収まった」に数えていた
（2026-09-15、6784）。

**見張りは行ごとに置く。** `resolve` は「収まった」「超えた」「判定できない」
の3つに分ける。**判定できなかった行を分母に入れない**——入れると割合が甘く
なり、確かめられなかったことが合格に化ける。

### 落ちようのない検査を「合格」と読まない

**広すぎる幅は、狭すぎる幅より悪い。** 狭ければ誤報が出て気付く。広いと
「全部合っている」と出て、**確かめたつもりになる。**

丸めの幅を「いちばん粗い桁」から出したら、全9列が `±0.05` になった。`PBR`
は 1 前後なので**相対 5%**。ずれの99%点は 1.3% なので、何を入れても 100%
収まる。**収まったのではなく、収まらないことがありえなかった。**

原因は末尾の 0 である。`25.10` は `25.1` と書かれる。1件でもそう書かれれば
`min()` は 1 桁と読む。**いちばん多い桁**を採る。

そして**見張りを比で置かない。** 「幅がずれより一桁以上広ければ警告」は
鳴らなかった——桁が粗ければ**ずれも一緒に広がる**ので、幅 50%・ずれ 13.8%
でも比は 3.6 倍にしかならない。**絶対値で見る**（`RESOLVING_LIMIT`）。

検査を作ったら、**それが落ちる条件を1つ実際に作って、落ちることを見る。**

### 「要る期数」の表に、検出できる差を並べない

「この設計で検出するのに要るイベント数」の表に、**検出できる差そのもの**を
「検出したい効果」として入れていた（2026-09-20、ユーザーが発見）。

**それに要る期数は、定義上いま在る期数そのものである。** 答えが必ず
「ちょうど足りる」になる行で、**何も言えない**——`CLAUDE.md`「落ちようのない
検査を『合格』と読まない」の表版である。

しかも #15 では、コミットした線（1.2%）と検出できる差（1.2043%）が偶然ほぼ
一致した。**同じ「1イベント 1.20%」が2行並び、「いまとの差 +4」が「あと4件で
足りる」に読めた。** 実際の取り高は **−2.48%** で、**閉じた理由は向きである。**

2つ直した。

- **並べるのは「選んだ大きさ」だけ。** コミットした線と、（正なら）見込みの
  下限。**検出できる差は入れない**
- **向きが逆なら、表を出さない。** 「期数の問題ではない。増やしても、この
  向きのままなら通らない」と書く。**仮の数字は仮と分かる形でだけ添える**

**表の見出しが約束していることと、行が答えていることを突き合わせる。**
見出しは「何段階か並べて比べる表」に見えるのに、中身は2行とも同じ値だった。

#### 同じ行の2つの列が、別々の標本を指していないか（**翌日。2026-09-20**）

上を直した**次の回**に、同じ表の隣の列でやった。ユーザーが見つけた。

要るイベント日 1,375 に対し、**年数の列は IS の率**（992日÷5.0年）で 6.9年、
**「いまとの差」の列は OOS の日数**（1,861）と比べて「足りている」。脚注は
さらに「**手元は IS 5.0年で 994 イベント日**」と書いていた。

**1つの行に、2つの標本が入っている。** IS の枠で読めば 994 < 1,375 で足りて
おらず、OOS の枠で読めば足りている。**出力がどちらにも読めて、答えが食い違う。**

しかも**閉じた理由が変わる**——IS の枠なら「観測不足」、OOS の枠なら
「検出力はあり、効果が線に届かない」。

- **率は `power.requirement` が `periods ÷ period_years` から作る。**
  `Requirement.__post_init__` が**2つの比が一致すること**を見るので、
  混ぜた行は作れない
- **「いま在る」を表の行に入れた。** 外（脚注）に置くと、読む側が別の標本の
  数字と突き合わせる。**それがこの事故そのものだった**
- 引数を `span_years`（どの窓か言っていない）から **`period_years`** に変えた

**そして、前の回に書いたテストが、この不具合を固定していた。**
`TestTheYearsColumnUsesTheSameUnitAsThePeriods` という名前で、中身は
`assert "per_year = len(values) / span_years" in body`。**名前は「`periods`
と同じ単位で数える」と言い、assert は逆を守っていた。**

**テストの名前が主張していることと、assert が守っていることを突き合わせる。**
表の見出しでやったのと同じ照合を、テストにも当てる。

#### 「検出力が足りている」は、効果の大きさを言わないと意味が無い

#16 を閉じたとき、**「検出力は足りている。効果が線に届かなかった」**と
報告し、事前登録にも一覧にもそう書いた。**言い過ぎだった**（2026-09-20、
ユーザーが発見）。

| もし真の効果が | 要る | 手元 8.7年 |
|---|---|---|
| コミットした線 1.20% | 6.4年 | 足りる |
| IS の中心 +0.75% | 16.5年 | **足りない** |
| **IS の下限 +0.04%** | **5,726年** | 話にならない |

**同じ設計で、答えが3通りある。** 私は1行目を見て「足りている」と書いた。
**§0 が見るのは3行目**で、#7 が真ん中を見て沈んだからそうなっている。

**そして「そこに無かった」も言い過ぎだった。** 帯の上端は +1.45% で、線
1.20% を超えている。**「線を下回る」ことは示せていない。** 示せたのは
「上回るとは言えなかった」まで——**判定規則を、効果の不在と読み替えていた。**

**規則が「通さない」と言ったことと、世界が「無い」ことは別である。**
`CLAUDE.md`「『差が無い』ではなく『差があるとは言えない』」の、ゲート版。

#### 表に2つの大きさを並べたら、どちらが関門かを行に書く

上の誤読は、**表がそう出ていた**から起きた。「1イベント 0.04% → 5,726年」
と「1イベント 1.20% → 6.4年 / 足りている」が並び、**6.4年 のほうが読まれる。**

**注意書きでは止まらない。** `_needed_table` が行に
**「§0 の関門」／「参考」**と書く。`power-gate` の同じ表もそこを呼ぶ
——**2つ持つと、片方だけ直したときに同じ誤読が残る。**

**差の列も消した。** 「+4」が「あと4件で足りる」に読めた回がある（#15）。
**要る数といま在る数を並べれば、引き算は読む側でできる。**

**そして下限が 0 以下で関門の行が出ないときは、その旨を1行出す**——参考の
行だけ残ると、また同じ誤読になる。

#### モジュールの頭に、同じ名前の別物が居ないか

上を直すとき `period_years=_judgement_years(OOS_FROM, OOS_END)` と書いた。
**`cli` はモジュールの頭で `pead` の `OOS_FROM`（2024-01-01）を import して
いる。** `gap_fill` のそれは 2018-01-01 で、**6年半ずれる。**

**例外は出ない。年数の列が 3倍になるだけである。** ruff が `OOS_END` の側だけ
を `F821` で拾って気付いた——**`OOS_FROM` のほうは解決してしまうので、何も
言われなかった。**

`tests/test_deferred_imports.py` は**名前が実在するか**を見る。ここは実在
する。**「解決すること」は「正しいものを掴んだこと」ではない。**
`tests/test_cli.py` が AST で**どこから束ねたか**を見る。

### 「見ること」と書いただけで、見る道具を置いていないか

`#16` が急落 819 件を権利落ちで外し、警告が **「中身を見ること」** と出した。
**そこで止まっていた**（2026-09-20、ユーザーが指摘）。指示が出ているだけで、
**見る手段が無い。** 件数しか返していないので、外したものが何だったのかを
後から調べようがなかった。

**警告に次の一手を書くなら、その一手が打てる形にして返す。** `KnifeEvents`
は外した急落そのものを持って返し（`__post_init__` が件数と数を突き合わせる）、
`checks\権利落ちの日は合っているか.bat` が中身を見る。

**そして、警告の文面が説明を1つ落としていた。**

> 20% の下げを配当では作れないはずなので、**特別配当か、`ExDate` の読み違い
> である。**

**配当が 20% を作る必要は無い。** 除外の窓は急落の6営業日なので、**19% 下げた
銘柄を 20% の線の向こうに押し出せば足りる。** しかも「20% 下げた」で絞る時点
で、**押し出された側が選ばれる。**

**「その旗が立ったこと」の意味も、立たない場合と同じだけ考える。**
`CLAUDE.md`「『ゼロでない』を根拠に断定しない」の裏側である——あちらは
**立たないほう**、こちらは**立ったほう**を決め打った。

**窓が長いほど、同じ規則が広く網をかける。** `#15` は窓が1日なので同じ規則で
網は狭い。そこから素直に持ってきたのが原因だった。**規則を移すときは、移した
先の窓の長さで網の広さが変わる。**

#### 数えただけで、直したことにしない

同じ回で、**額 0 の権利落ちで急落 371 件を外している**ことを数え、
**その表を出して、直さずに次の話に移った**（2026-09-20、ユーザーが指摘）。
「まだ直っていません（今回はそれを数えただけです）」。

**測る道具を作ると、作ったことで一区切りついた気になる。** 数は出ている
ので「分かっている」状態ではあるが、**コードは前のままである。**

**見つけたものは、直すか、直さない理由を書くかのどちらかにする。**
ここでは 371 件（額 0）は**直し**、190 件（配当を戻しても急落）は
**直さない理由を事前登録に書いた**——後者は間違いの訂正ではなく設計の選択で、
**数字を見た後に選ぶことになる**からである。

#### 「分からない」と「0 と書いてある」を、行単位で混ぜない

額 0 の権利落ちで外すのをやめた**つもりだった**。実データで 371 → **81 件**
に減っただけで、0 にならなかった（2026-09-20、ユーザーが2度指摘）。

```python
if rate is None:
    unknown[key] = ...  # ← 他の行が「0」と言っていても、こちらが勝つ
```

同じ権利落ち日に**平均2.4行**ある。「額が空の行」が1つでもあれば未公表扱いに
なり、**落とすのは全部の行が 0 のときだけ**になっていた。**別の行が 0 と
言っているなら、分かっている。**

**鍵ごとに畳んでから判断する。** 行を見ながら分類すると、**あとから来た行が
前の行の情報を上書きする。** `positive` / `has_amount` を先に集めて、
**「額が1行も無い」ときだけ未公表**にした。

#### 代理で外すより、先に落として測る（**順序**）

`#16` は「機械的な値下がりを外す」ために「**権利落ちが窓に在れば外す**」と
書いた。**それは代理である。** 実データで、**外すために置いた規則が本物の
急落を 189 件巻き込んでいた**（2026-09-20、ユーザーが指摘）。

**順序を入れ替えれば、規則そのものが要らない。**

| | |
|---|---|
| 代理 | 窓に権利落ちが在る → 外す（**本物も巻き込む**） |
| 本体 | **配当を落としてから線を当てる**（押し出しが起きない） |

**「外す」は、測れないものへの逃げ道である。** 測れるなら測って引く。
外すのは**引けないものだけ**（ここでは額が一度も公表されていない権利落ち）。

そして**片側だけ直すと非対称が残る。** ショートは配当を払う側なので、
急落側だけ落として保有側を放置すると**取り高が高く出る**（+0.008%/件）。
`event_sample` に `adjust` を1つ足し、**両方を同じ値で測る。**

**既定は素通しにする。** 他の説（#5・#8・#15）は1行も変わらない。

#### 増えようのない向きは、フィールドではなく不変条件にする

配当を落とすと急落が「増えた／減った」を両方数えるつもりで書いた。
**増えることはありえない**——窓の中の権利落ちは分母（基準日）だけを下げる
ので、**落とせば下げは必ず浅くなる。**

**常に 0 のフィールドは、何も区別しない。** 数えるのをやめて、
**増えたらその場で落とす**不変条件にした。テストは**逆向きの調整を差し込んで**
落ちることを確かめる——本物の入力では起こせないので、**壊れた実装のほうを
作って当てる。**

#### 理屈で成り立つ不変条件が、浮動小数でも成り立つとは限らない

「配当を落とせば下げは必ず浅くなる」——窓の中の権利落ちは分母（基準日）
だけを下げるので、**増えることはありえない。** そう書いて、**増えたら落ちる**
不変条件を置いた。**ユーザーの PC で落ちた**（2026-09-20、2461）。

**線の上にちょうど乗っていたからである。** `100000 → 80000` はちょうど
−20% だが、float では `-0.19999999999999996`——**わずかに線を超えない。**
両辺に同じ倍率を掛けると `-0.20000000000000007` になり、**事象になる。**

**そして「事象になる」ほうが多い。** 20万通り試すと、転んだうちの 87% が
そちら側だった。日本株は `1000 → 800` のような**丸い値がよく出る**ので、
2万件に1件は起きる。

**比較の向きではなく、量で見る。** 「増えた」で落とすのをやめ、
**「本当に深くなった」（1e-9 を超えて）** で落とす。同点は別に数えて出す
——**隠すと、次に本物が混ざったとき区別できない。**

**閾値に等号を置いたら、その上にちょうど乗る入力を疑う。** 丸い数字は
実データに多い。

##### そして本丸は、線の当て方そのものだった（ユーザーが見つけた）

上は監査が落ちた話だが、**もっと効くのは「ちょうど −20% が急落に数えられて
いない」ほう**だった。事前登録は「**−20% 以下**」と書いてあるのに、

```python
after / before - 1.0 <= -drop  # 544/680 → -0.19999999999999996 で落ちる
```

**割り算の丸め1回で、等号が消えていた。** 人工の値動きで数えると
**急落 99 件のうち 2 件**——2%。

**しかも同じ書き方が、定数によって「以下」になったり「未満」になったり
する。** `-0.20` は線を外し、`-0.03`（#15 の下窓）は外さない
（線上の組で 40,142 対 0）。**#16 だけが静かに取りこぼしていた。**

```python
later <= reference * (1.0 - drop)  # 680.0 * 0.8 は厳密に 544.0
```

**割り算をやめると消える。** `backtest/fall.py` に1つだけ置き、`#15` と
`#16` が両方そこを呼ぶ——**同じ式が2つ在って、片方だけ壊れていた。**
`tests/test_fall.py` が、**コードの中に古い式が戻るのを止める**
（説明の散文は見ない。`tokenize` でコメントと文字列を落として走査する）。

**イプシロンは足していない。** 幅を推測で置くより、**式を変えるほうが根拠を
書ける。** 残る限界（`1.0 - drop` 自体が真の値のわずか上）は**書いておく**
——どの呼び値よりも細かく、事前登録の「以下」に倒れる側である。

#### 調整した値を、調整と関係ない判断に使わない

配当を落とした終値を、そのまま**流動性の絞り**にも渡していた。
**配当調整はリターンのためのもので、規模のためのものではない。**

落とした値で売買代金を測ると実際より小さく出る。しかも**倍率は古い足ほど
小さい**——14年・年2回・利回り 1.3% なら、いちばん古い足は **0.69倍**。
**1億円の線の上下にいる銘柄が、時期によって違う基準で落ちる。**

**例外は出ない。件数が静かに減るだけである。**

**気付いたのはユーザーで、根拠は件数の食い違いだった**（2026-09-20）——
除外を 303 件やめたのに事象が 169 件**減った**。監査の見立て
（配当が作った 98 件が消え、本物の 189 件が戻る）と逆向きである。

**「数が思った向きに動かない」を、そのままにしない。**

#### 片方が落ちて片方が通ったとき、通ったほうを根拠にしない

同じ調整を通して、**監査は落ち、測定は最後まで走った。** 「測定が通った
のだから調整は正しい」と読みたくなるが、**測定側は前後を比べていない**
——破れる不変条件をそもそも持っていない。

**通ったのではなく、見ていない。** そして実際、両方に同じ欠陥
（流動性を調整後の値で見る）が乗っていた。

#### 最終データで、過去の判断を裁かない

残った 81 件に「**額 0 で外した（外すべきでなかった）**」と札を付けていた。
**外した時点では配当が公表されていて、後から無配へ訂正されたもの**である。

除外は**その日までに公表された情報**だけで行う（先読みを入れないため）。
**監査だけが最終データを見ているので、同じ表に2つの情報集合が混ざる。**

**「その日に知りようがなかったこと」で過去の判断を裁かない。** 別の欄に置き、
**「外すべきでなかった」の分子にも分母にも入れない。**

#### 「〜のはず」と書いたら、それを測る欄を作る

保有窓の権利落ちを数える欄が、**「落とした後なので、押し上げは消えている
はず」と書きながら +0.007%/件 を出していた**（2026-09-20、ユーザーが出力の
自己矛盾から指摘）。

**その数字は残っている量ではない。** 数えていたのは**窓の中に在る配当の
大きさ**（41件 × 0.90% ÷ 5,596件）で、**価格を調整したかどうかを1度も
見ていない。** 札が、測っていないことを主張していた。

**主張を測定に変える。** 同じ窓のリターンを**調整の前と後で引いた差**を
出し、窓の中の配当と釣り合うかを見る。

| | 意味 |
|---|---|
| `drag` | **調整しなければ乗っていた**押し上げ |
| `removed` | **調整が実際に抜いた**分 |

**釣り合えば測れたことになる。0 なら当たっていない。2倍なら二重。**
テストは3つとも置く——釣り合う形、渡し忘れた形、二重に抜いた形。

**「はず」は、確認していないことの目印である。**

##### そして、その欄は初回で本物を捕まえた（**尺度違い**）

上で置いた `removed` が、**実データに初めて当たった回で鳴った**——窓の中の
配当 **+0.007%/件** に対し、抜けたのは **+0.036%/件**。**5.1倍**である
（2026-09-20、ユーザーが「スクリプト自身が『2倍なら二重』と書いているのに
5倍だ」と指摘）。

**「消えているはず」と書いていたときは、この食い違いが見えていなかった。**

**最初に立てた筋は外れだった。** 「`drag` が中央値で `removed` が平均では
ないか」——再現させたら鳴らなかったので捨てた。**もっともらしい筋を1つ
思い付いて、確かめずに直して押す**のを、ここでは踏まずに済んだ。

原因は**割る相手**だった。

```python
before = close[position - 1]  # ← split_adjusted 済みの終値
ratio = rate / before  # ← rate は円建て（調整前の尺度）
```

**円建ての配当額を、分割調整後の終値で割っていた。** 同じファイルの
`_sessions` には「**利回りは調整前で割る**」と書いてあり、監査の `_yield_on`
はそのとおり生の終値で割っている。**2箇所で割る相手が違っていた。**

1:10 の分割を挟む盤面で再現すると、利回り **1.0% が 10.0%** になる。
**分割より前の権利落ちは全部、分割比のぶん余計に落ちていた。** 日本株は
1:2 の分割が多いので、稀な端ではない。

**`base` を必須の引数にした。** 既定を作れば、また黙って混ざる。

#### ユーザーの筋も、議論ではなく置き方で切り分ける

同じ回に「`removed` は急落を作った側の5営業日まで拾っているのでは。だとすれば
**二重ではなく、別のものを数えている**」という筋が出た。**もっともらしい。**

**置く日だけを変えた盤面で答えが出る。**

| 権利落ちを置いた日 | `taken` |
|---|---|
| 急落を作った窓の中・急落日・**entry 当日** | **0.000000%** |
| 保有窓の中・exit 当日 | +1.010101% |
| 保有窓より後 | 0.000000% |

**保有窓の外は1つも拾っていない。** 理屈も付く——倍率は「その日より前の足」
に掛かるので、権利落ちが entry 以前なら **entry と exit の両方に同じ倍率が
掛かって完全に相殺する。**

**筋が外れていても、切り分けは無駄にならない。** この表を作る過程で、
**`drag` だけが entry 当日を数えている**ことが出た——`taken` は 0 なのに。
**2つの列が1日違う窓を見ていた。**

**正しいのは `taken` のほうである。** 権利落ちが D+1 なら株価はその日の
**寄付きで**落ち、**こちらはその寄付きで入る**ので、落ちた後の値段で入って
いる。配当は最初から乗っていない。

**分母も違っていた。** `drag` は全イベント、`removed` は窓が最後まで在る
イベントだけで平均していた。**同じ行の2つの列が、別々の標本を指していないか**
——3度書いてある形の4度目である。**両方から外して、`measured` を出す。**

##### 直したら 5.1倍が 3.0倍になった。**「片方だけ直った」を残さない**

尺度違いを直して回したら、**釣り合わないまま 3.0倍に縮んだ**（2026-09-20、
ユーザーが「出力自身が『2倍なら二重』と書いているのに 3.0倍はその外側だ」と
指摘）。**縮んだことを「直った」と読まない。**

**原因は2つあった。** 2つ目は**入力そのものが別**だったことである。

| 列 | 何から作っていたか |
|---|---|
| `drag` | `ex_dividend_rates`——**監査専用。最後に公表された値（先読みあり）** |
| `removed` | `ex_dividends_known_by`——**先読みを外した、最初の正の公表** |

**docstring にそう書いてあった。** 読まずに並べていた。**後から 0 へ訂正
された配当**は、`drag` では 0、`removed` では正になる。**同じ行の2つの列が、
別々の標本を指していた**——5度目である。

**さらに式の形も違った。** 抜ける量は `窓の総リターン × (1/∏(1−利回り) − 1)`
で、**利回りの単純和ではない。** 0.6 なら 1.5 になる。

**そして絞りが片方にしか掛かっていなかった**——`_yield_on` の `MAX_YIELD`
（50%）は `drag` 側だけで、`dividend_adjusted` は `0 < 利回り < 1` を見る。

3つとも**同じ入力・同じ式・同じ絞り**に揃えた。揃えたので**幅も狭めた**
（25% → 1e-6）。**広い幅は、量そのものが違っていることを中に隠す。**

###### 揃えた拍子に、渡し忘れの検査が死んだ

`drag` と `removed` を同じ入力から作ると、**`paid` を渡し忘れたとき両方 0 に
なって釣り合う。** 前はそこで鳴っていた。**テストが落ちて気付いた。**

**独立な相手で捕まえ直す。** 最終データ（先読みあり）に配当が在るのに
`drag` が 0 なら、渡し忘れか未公表である。**最終データはここでだけ使い、
判定には混ぜない。**

**直したあと、直した先で何が変わったかも見る**——書いてある規則である。
ここは落ちたので気付いたが、**落ちずに検査が死ぬ形もありうる。**

##### 「まず起きない」と書いて、53 件起きた

`DividendAdjustment.impossible`（配当が前日終値以上）の docstring に
**「正しい分母で割ればまず起きない」**と書いた。分母を直した後の実データで
**急落側 53 件・保有窓側 17 件**残った。

**書いた前提は、成り立つことを確かめるか、前提が要らない形に変える**
——`identity_check` で踏んだのと同じ形である。断定を消し、**原因はまだ
分かっていないと書いた。**

そして**中身を持って返るようにした。** 件数しか出ていなかったので、
**ユーザーの側からどちらの読み違いか追えなかった。**「中身を見ること」と
書いて見る道具が無い、を2度やらない。

##### 合計だけ出すと、その中に紛れる

`保有窓の配当: 当てた 12,156 件、当てなかった 2,264 件` と出していた。
**15.7% が当たっていないのに、理由ごとの数が出ていない。**
`DividendAdjustment` は5つに分けて持っているのに、**表に出していなかった。**

**列ごとに独立に数える**——#5 で会計年度末が 72,156 件すべて読めていな
かったのが表の1行にしか出なかったのと同じ形である。

#### 測った件数を、文面に焼き付けない

`ex-date-audit` の見出しが **819 件のまま2世代古かった**（ユーザーが発見）。
実際に外しているのは 368 件である。**数字が固定文言に入ると、次に変わっても
そこだけ古いまま、しかも古いと分からない。**

`docs/PASSING.md` を生成物にしているのと同じ理由である。**その回の出力が
数えるものを、先に書かない。** `tests/test_launchers.py` の
`COUNTS_IN_PROSE` が、**新しいランチャーに件数が入るのを止める**
（減る方向にしか動かない一覧。直したら消す）。

#### 早期 return が、下に足した検査を黙らせる

`Alignment.warnings()` に「額 0 の群にも段差が無いか」を足したら、
**1行も鳴らなかった。** 先頭に `if not self.events: return [...]` が在り、
額 0 しか無い盤面ではそこで抜けていた——**足した検査はその下に在った。**

**docstring には「早期 return しない」と書いてあった。** 書いた本人が、
**その関数の先頭に在る return を見落として下に足した。**

**検査を足したら、その検査だけが鳴る盤面を1つ作って、鳴ることを見る。**

#### 日本語の札は、rich から見れば1語である

表の札が `…` で切れた。**消えたのは「（外すべきでなかった）」**——
**意味はその末尾に在った**（2026-09-20、ユーザーが2度指摘。2度目は
「件数が 0 になったので実害は減ったが、幅の問題自体は残っている」）。

日本語には空白が無いので、rich は札全体を**折り返せない1語**として扱う。
列に入らなければ `overflow`（既定は `ellipsis`）が働く。**`overflow="fold"`
は同じファイルの `_event_gate` で既に使っていた。**

2つ直した。

- **札を短くする。** 説明は警告の行に置く（そちらは `console.print` が
  折り返す）。**表の札に文章を入れない**
- **`overflow="fold"`** を当てる

そして**幅を決めて刷るテスト**を置いた。`Console(width=80/100/120)` で
`…` が出ないことと、**末尾の「外すべきでなかった」が3回出ること**を見る。

**`--help` の幅に賭けるのとは別である。** あちらは端末の幅が手元と CI で
違うのが問題だった。ここは幅を固定するので、どこで走らせても同じ答えになる。
**「描画結果を見ない」ではなく、「環境で変わるものに賭けない」である。**

### 許容幅を推測で置かない。桁は原本に書いてある

`PER × EPS` と `PBR × BPS` の一致を「1% 以内」で見ていた。**1% に根拠は
無かった。** 実データで 96.9% 一致と出て、残り 3.1% を「列の意味が違う」と
読んだ。

効いていたのは `PBR` の丸めである。`PBR` は 1 前後なので、**小数2桁なら
相対誤差は ±0.5%。** 決め打ちの 1% は、その裾をちょうど切っていた。桁から
幅を出すと、同じデータで **100% が収まる。**

**幅を決め打つときは、その数字がどこから来たかを書く。** 書けないなら、
測れるところから出す。

### 「ゼロでない」を根拠に断定しない

同じ出力で2つやった。

- 「会社予想の EPS でなら合う行が**1行でもあれば**列の取り違え」——実際は
  合わない行の 2.5%、判定した行の 0.08% だった。取り違えなら**ほとんどが**
  救われるはずである。
- 「ずれが小さい EPS に偏っていなければ丸めではない」——**丸めの出どころを
  `EPS` だと思い込んでいた。** `PBR` の丸めは `EPS` の大小と関係が無い。
  平らであることは、何も否定しない。

**件数ではなく割合を見る。そして「その旗が立たないこと」が何を意味するかを、
立つ場合と同じだけ考える。**

### 収益率どうしを引き算しない

指数 +191.3% と ETF +197.1% を引いて「差 +5.8%」と出していた。水準は
2.913倍と 2.971倍なので、**比は +2.0%。差を 2.9倍に水増ししていた**
（2026-09-16）。

引き算が合うのは収益率が小さいときだけである。**「尺度の違う値を組み合わせる」
——このプロジェクトが繰り返している形そのものだった。**

### 散らばりを見ずに平均を出さない

年あたり +0.11% が残ったが、年ごとの差は −1.38% から +0.62% まで振れていた。
平均 +0.045%、95% の幅は **−0.16% 〜 +0.25%** で、**0 も信託報酬ぶんの負の
値も、その中にある。**

**「差が無い」ではなく「差があるとは言えない」。** 両者は別で、前者は測って
言い切ったこと、後者は測ったが言い切れなかったことである。

そして**1点から確かさを名乗らない。** 年が1つだと散らばりが測れず幅が点に
なり、「0 をまたがない」が必ず成り立つ——どんな値でも区別できることになる。

### 独立な観測を、件数で数えない

イベント型の散らばりは**日ごとの等加重バスケット**で測る（同じ日のイベントを
まとめないと、重なった日だけ重みが増える）。そこまでは正しかった。

**検出できる差を、日数ではなく件数で割っていた。** #5 は 1,827 件が **831 日**
にまとまっていて、**n を 2.2倍に水増ししていた。** 検出できる差はその平方根
ぶん、**1.48倍甘く出る**（2026-09-17）。

**系列を作るときの単位と、検出力を計算するときの単位を揃える。** どちらも
「1つの観測」と呼べてしまうので、**例外は出ない。**

#### 同じ列に、2つの単位を並べない（**3度目。2026-09-19**）

権利落ちの表で、**「別々の権利落ち 114,942（銘柄 × 日）」のすぐ下に、
「IS 92,831 / OOS 206,515」を行で数えて並べていた。** 足すと 299,346 で、
**2.6倍ずれている。** ユーザーが見つけた。

**そのときは §0 に流れていなかった。** それでも同じことである——表がそう
読めるように出ている時点で、**次に誰かが「判定に使える期数」として読む。**

**注意書きでは止まらない。** 内訳を**足して合う形**にして、
**合わなければ生成時に落とす**（`ExDateCoverage.__post_init__`。
`EventSample` と同じ作り）。3つ目の入れ物（判定期間より後の権利落ち）が
要るとそこで分かる——**黙ってどちらかに混ざる形が消える。**

### 同じ処理を2つ書かない

`key_period` を、それを戒める文章を書いた同じ日に2つ目を書いた。正本は
`jquants_archive.py` にある。**呼ぶ側で書き直さない。**

### テストが通ったことを、正しいことの証拠にしない

差し替えたはずのモックが効いておらず、**空の辞書を素通しして緑になっていた**
（`build_events` は関数の中で import されている）。出力そのものを見るほうが
強い。**緑であることは、見たことを意味しない。**

## 品質の姿勢

このプロジェクトで繰り返し起きる不具合は「例外で落ちる間違い」ではなく **「もっともらしいが違う値が黙って出る」** 種類のもの（例: 分割前後で尺度の違う値を組み合わせる、連結と単体を取り違える、古いコードのまま実行して気付かない）。

- **部品だけでなく、組み立てを1本通すテストを必ず置く。** `factor_panel` は
  部品（signal の符号・合成の標準化）を13個テストしていたのに、`build_panel`
  自体を一度も呼んでいなかった。存在しない引数（`list_securities(session,
  market="JP")`）が本番の実行まで出て行き、**その間テストは全部緑だった。**
- 新しいデータ取得ロジックは、可能な限り実データ（実際のファイル・実際のAPIレスポンス）で検証する。fixtureは実物から作る。
- 数字を1つ検証したら終わりにせず、別の切り口（別の計算式、別の銘柄、別の期間）で同じ数字を出して一致するか確かめる。
- コード側のバージョンを確認する手段（`uv run stock-ai info` の `version` 行、各 `.ps1` の `Show-Version`）を必ず用意し、ユーザーが古いコードを実行していないか本人にもこちらにも分かるようにする。

## コミット・PR

- コミットメッセージ・PR本文・コード中のコメントに、使用したモデル名を含めない。
- モデル名を聞かれたら `get_session`（claude-code-remote MCP）で `session_context.model` と `external_metadata.last_served_model` を確認して答える。
