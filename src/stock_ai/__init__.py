"""stock-ai: AI-driven stock screening, backtesting, and trading system.

The package is organised as a pipeline of independent layers
(data -> analysis -> scoring -> execution -> notification -> dashboard),
wired together through abstract interfaces to keep each layer swappable.
"""

__version__ = "0.1.0"
